---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: gametemplates.kestrel.gg
spec:
  group: kestrel.gg
  names:
    kind: GameTemplate
    listKind: GameTemplateList
    plural: gametemplates
    shortNames:
    - gtmpl
    - gametmpl
    singular: gametemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.game
      name: Game
      type: string
    - jsonPath: .spec.version
      name: Version
      type: string
    - jsonPath: .status.inUseCount
      name: In Use
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          GameTemplate is a cluster-scoped blueprint for a specific game. A
          GameTemplate is distributed as part of a Kestrel module (OCI artifact)
          and instantiated via GameServer.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              GameTemplateSpec defines a reusable blueprint for a game server.
              Users instantiate a GameTemplate by creating a GameServer that
              references it.
            properties:
              accentColor:
                description: |-
                  AccentColor is an optional brand color (CSS hex, e.g. "#3b82f6")
                  the dashboard uses to tint this game's icon and accents. When
                  empty the dashboard falls back to a neutral default. This replaces
                  the previously hardcoded per-game color palette in the web app, so
                  new games carry their own color without a frontend change.
                pattern: ^#[0-9a-fA-F]{6}$
                type: string
              agent:
                description: Agent tunes the sidecar deployed alongside the game container.
                properties:
                  image:
                    description: |-
                      Image overrides the default agent image. Normally set by the
                      operator to the image matching its own build.
                    type: string
                  resources:
                    description: Resources overrides agent resources.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                type: object
              args:
                items:
                  type: string
                type: array
              capabilities:
                description: |-
                  Capabilities declares the game-specific console commands behind
                  agent features (player moderation, backup quiesce). The operator
                  serializes this onto the agent sidecar, which interprets it at
                  runtime — modules add full feature support without agent code
                  changes. All commands run over the template's RCON connection,
                  so they require rcon.protocol != none.
                properties:
                  actions:
                    description: |-
                      Actions declares named operator actions surfaced as buttons on the
                      server detail page. Each runs a templated console command over the
                      template's RCON connection, so they require rcon.protocol != none.
                    items:
                      description: |-
                        ServerActionSpec declares a named operator action surfaced as a button
                        on the server detail page. Running it renders Command (a Go
                        text/template) with the collected parameters and sends the result over
                        the template's RCON connection.
                      properties:
                        command:
                          description: |-
                            Command is a Go text/template rendered with .Params (each declared
                            parameter name mapped to its resolved value) and sent over RCON,
                            e.g. "say {{.Params.message}}". Parameter values are sanitized for
                            console-injection before rendering.
                          minLength: 1
                          type: string
                        confirm:
                          description: |-
                            Confirm, when true, makes the dashboard require explicit
                            confirmation before running the action.
                          type: boolean
                        danger:
                          description: |-
                            Danger marks a destructive action so the dashboard styles it
                            distinctly (e.g. a red button).
                          type: boolean
                        description:
                          description: Description explains the action; shown in the
                            confirmation dialog.
                          type: string
                        displayName:
                          description: DisplayName is the button label.
                          minLength: 1
                          type: string
                        icon:
                          description: Icon is an optional lucide-react icon name
                            (e.g. "megaphone").
                          type: string
                        id:
                          description: ID is a stable identifier, unique within the
                            template's actions.
                          maxLength: 63
                          pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                          type: string
                        params:
                          description: Params declares inputs collected from the user
                            before running.
                          items:
                            description: ActionParamSpec is a single input collected
                              before running an action.
                            properties:
                              default:
                                description: Default is the pre-filled value (as a
                                  string).
                                type: string
                              description:
                                description: Description explains the input.
                                type: string
                              displayName:
                                description: DisplayName labels the input in the UI.
                                type: string
                              enum:
                                description: Enum restricts valid values when Type=enum.
                                items:
                                  type: string
                                type: array
                              name:
                                description: |-
                                  Name is the parameter identifier referenced in the command
                                  template as {{.Params.<name>}}.
                                maxLength: 63
                                pattern: ^[a-zA-Z_][a-zA-Z0-9_]*$
                                type: string
                              required:
                                description: Required, when true, blocks running until
                                  the input is set.
                                type: boolean
                              type:
                                default: string
                                description: Type controls the input widget and validation.
                                enum:
                                - string
                                - int
                                - bool
                                - enum
                                type: string
                            required:
                            - name
                            - type
                            type: object
                          maxItems: 16
                          type: array
                      required:
                      - command
                      - displayName
                      - id
                      type: object
                    maxItems: 32
                    type: array
                  lifecycle:
                    description: |-
                      Lifecycle configures graceful lifecycle transitions the agent drives
                      over RCON — currently the in-game stop sequence the operator runs
                      before scaling the server down, so the world is saved and shut down
                      cleanly instead of relying on a container SIGTERM. Requires
                      rcon.protocol != none.
                    properties:
                      stop:
                        description: |-
                          Stop runs, in order, to shut the game down cleanly before the
                          operator scales the server to zero (e.g. ["stop"] for Minecraft, or
                          a warn-then-shutdown sequence). The operator issues these, waits for
                          the game to go not-ready (or for GameServer.spec.stopGracePeriodSeconds
                          to elapse), then removes the pod — so a SIGTERM never interrupts an
                          in-progress save.
                        items:
                          type: string
                        maxItems: 16
                        minItems: 1
                        type: array
                    required:
                    - stop
                    type: object
                  mods:
                    description: |-
                      Mods declares how the dashboard manages this game's mods/plugins.
                      The dashboard is generic — it lists, installs, and removes mods by
                      calling the agent; this block tells the agent where mods live and
                      how installs are permitted. Mods are files under a directory on the
                      game's data volume, so this needs no RCON.
                    properties:
                      extensions:
                        description: |-
                          Extensions optionally restricts which files in Path are treated as
                          mods (e.g. [".jar"]). Empty lists every file. Used with Path; per-
                          loader extensions are set on each ModLoaderSpec instead.
                        items:
                          type: string
                        type: array
                      extract:
                        description: |-
                          Extract is the resolved per-loader Extract flag (the operator copies
                          it from the active loader). When true the agent unpacks archive mods
                          into per-mod folders. Not normally set by template authors at this
                          level — set it on the loader entry instead.
                        type: boolean
                      install:
                        description: |-
                          Install, when set, lets the dashboard add new mods by downloading
                          them into the resolved mods directory. When unset, only listing and
                          removal are offered. Shared across all loaders.
                        properties:
                          allowedHosts:
                            description: |-
                              AllowedHosts is the allowlist of hosts the agent may download from:
                              an exact hostname ("cdn.modrinth.com") or a leading-dot suffix
                              (".modrinth.com") matching that host and its subdomains. Downloads
                              from any other host — and any that resolve to a private, loopback,
                              or link-local address — are refused (SSRF guard).
                            items:
                              type: string
                            minItems: 1
                            type: array
                          maxSizeMB:
                            description: MaxSizeMB caps the download size in mebibytes.
                              Defaults to 256.
                            format: int32
                            maximum: 4096
                            minimum: 1
                            type: integer
                        required:
                        - allowedHosts
                        type: object
                      loaders:
                        additionalProperties:
                          description: |-
                            ModLoaderSpec is the mods directory for one loader / server-type,
                            selected by the active GameVersion.loader. The operator provisions a
                            per-(version+loader) PVC and mounts it at storage.mountPath/<Path> on
                            both the game container (where the image reads mods) and the agent.
                          properties:
                            displayName:
                              description: DisplayName labels this volume in the Mods
                                tab, e.g. "Plugins (Paper)".
                              type: string
                            extensions:
                              description: |-
                                Extensions optionally restricts which files are treated as mods for
                                this loader (e.g. [".jar"], [".dll"]). Empty lists every file.
                              items:
                                type: string
                              type: array
                            extract:
                              description: |-
                                Extract, when true, tells the agent to treat downloaded mods as
                                archives (e.g. Thunderstore .zip): each install unpacks into its own
                                folder under the mods dir so the loader (e.g. BepInEx, which scans
                                recursively) finds the contained files. Listing/removal then operate
                                on those per-mod folders. Use for loaders distributed as archives.
                              type: boolean
                            path:
                              description: |-
                                Path is this loader's mods/plugins directory, relative to
                                storage.mountPath (e.g. "plugins", "mods", "bepinex/plugins",
                                "ModPacks"). Absolute paths and ".." segments are rejected. MaxLength
                                is kept small to bound the per-entry CEL validation cost (this rule
                                lives inside the loaders map, so its cost is multiplied per entry).
                              maxLength: 128
                              minLength: 1
                              type: string
                              x-kubernetes-validations:
                              - message: path must be relative to the data mount and
                                  must not contain '..'
                                rule: '!self.startsWith(''/'') && !self.contains(''..'')'
                          required:
                          - path
                          type: object
                        description: |-
                          Loaders maps a loader id (from GameVersion.loader) to its mods
                          directory. When the active version's loader has an entry here, the
                          operator provisions a per-(version+loader) PVC, mounts it at
                          storage.mountPath/<path> on the game and agent containers, and points
                          the agent's mod manager at it — so each version+loader keeps its own
                          mod set. When empty, Path is the single shared mods dir (legacy).
                          MaxProperties bounds the per-entry CEL validation cost (the apiserver
                          rejects the CRD otherwise).
                        maxProperties: 16
                        type: object
                      path:
                        description: |-
                          Path is the single shared mods directory, relative to
                          storage.mountPath (e.g. "mods" for Forge/Fabric, "plugins" for
                          Bukkit/Paper). Used when Loaders is empty. Absolute paths and ".."
                          segments are rejected.
                        maxLength: 256
                        type: string
                        x-kubernetes-validations:
                        - message: path must be relative to the data mount and must
                            not contain '..'
                          rule: '!self.startsWith(''/'') && !self.contains(''..'')'
                      registry:
                        description: |-
                          Registry, when set, lets the dashboard browse and search an external
                          mod registry for this game (in addition to install-by-URL). Kestrel
                          ships the provider engines and a generic browse UI; the module selects
                          a provider here and the agent's Install downloads the chosen file —
                          so the registry's CDN must also be in Install.AllowedHosts. Omit for
                          URL-only games (e.g. tModLoader, whose mods live on Steam Workshop).
                        properties:
                          providers:
                            description: |-
                              Providers is the ordered list of registries to offer. The dashboard
                              shows a provider switch when there's more than one; the first is the
                              default. A provider whose engine needs unmet config (e.g. a
                              CurseForge API key) is hidden until configured.
                            items:
                              description: ModProvider configures one registry engine
                                for a game.
                              properties:
                                community:
                                  description: |-
                                    Community is the Thunderstore community slug whose package index to
                                    search, e.g. "valheim". Required by the thunderstore provider;
                                    ignored by others.
                                  maxLength: 64
                                  type: string
                                modpacks:
                                  description: |-
                                    Modpacks, when set, surfaces a Modpacks browser for this provider and
                                    declares how installing one is applied. A modpack is selected as a
                                    whole (not added to the mods dir like a single mod), so install
                                    either pins it via a game-image env (RefEnv) or resolves and installs
                                    its dependency mods. Omit for providers without modpacks.
                                  properties:
                                    env:
                                      description: |-
                                        Env are additional fixed env applied alongside RefEnv when a modpack
                                        is active, e.g. {TYPE: MODRINTH} to switch the itzg image into
                                        Modrinth-modpack mode. Bounded to keep the CRD small.
                                      items:
                                        description: EnvVar represents an environment
                                          variable present in a Container.
                                        properties:
                                          name:
                                            description: |-
                                              Name of the environment variable.
                                              May consist of any printable ASCII characters except '='.
                                            type: string
                                          value:
                                            description: |-
                                              Variable references $(VAR_NAME) are expanded
                                              using the previously defined environment variables in the container and
                                              any service environment variables. If a variable cannot be resolved,
                                              the reference in the input string will be unchanged. Double $$ are reduced
                                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                              Escaped references will never be expanded, regardless of whether the variable
                                              exists or not.
                                              Defaults to "".
                                            type: string
                                          valueFrom:
                                            description: Source for the environment
                                              variable's value. Cannot be used if
                                              value is not empty.
                                            properties:
                                              configMapKeyRef:
                                                description: Selects a key of a ConfigMap.
                                                properties:
                                                  key:
                                                    description: The key to select.
                                                    type: string
                                                  name:
                                                    default: ""
                                                    description: |-
                                                      Name of the referent.
                                                      This field is effectively required, but due to backwards compatibility is
                                                      allowed to be empty. Instances of this type with an empty value here are
                                                      almost certainly wrong.
                                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                                    type: string
                                                  optional:
                                                    description: Specify whether the
                                                      ConfigMap or its key must be
                                                      defined
                                                    type: boolean
                                                required:
                                                - key
                                                type: object
                                                x-kubernetes-map-type: atomic
                                              fieldRef:
                                                description: |-
                                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                                properties:
                                                  apiVersion:
                                                    description: Version of the schema
                                                      the FieldPath is written in
                                                      terms of, defaults to "v1".
                                                    type: string
                                                  fieldPath:
                                                    description: Path of the field
                                                      to select in the specified API
                                                      version.
                                                    type: string
                                                required:
                                                - fieldPath
                                                type: object
                                                x-kubernetes-map-type: atomic
                                              fileKeyRef:
                                                description: |-
                                                  FileKeyRef selects a key of the env file.
                                                  Requires the EnvFiles feature gate to be enabled.
                                                properties:
                                                  key:
                                                    description: |-
                                                      The key within the env file. An invalid key will prevent the pod from starting.
                                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                                    type: string
                                                  optional:
                                                    default: false
                                                    description: |-
                                                      Specify whether the file or its key must be defined. If the file or key
                                                      does not exist, then the env var is not published.
                                                      If optional is set to true and the specified key does not exist,
                                                      the environment variable will not be set in the Pod's containers.

                                                      If optional is set to false and the specified key does not exist,
                                                      an error will be returned during Pod creation.
                                                    type: boolean
                                                  path:
                                                    description: |-
                                                      The path within the volume from which to select the file.
                                                      Must be relative and may not contain the '..' path or start with '..'.
                                                    type: string
                                                  volumeName:
                                                    description: The name of the volume
                                                      mount containing the env file.
                                                    type: string
                                                required:
                                                - key
                                                - path
                                                - volumeName
                                                type: object
                                                x-kubernetes-map-type: atomic
                                              resourceFieldRef:
                                                description: |-
                                                  Selects a resource of the container: only resources limits and requests
                                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                                properties:
                                                  containerName:
                                                    description: 'Container name:
                                                      required for volumes, optional
                                                      for env vars'
                                                    type: string
                                                  divisor:
                                                    anyOf:
                                                    - type: integer
                                                    - type: string
                                                    description: Specifies the output
                                                      format of the exposed resources,
                                                      defaults to "1"
                                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                                    x-kubernetes-int-or-string: true
                                                  resource:
                                                    description: 'Required: resource
                                                      to select'
                                                    type: string
                                                required:
                                                - resource
                                                type: object
                                                x-kubernetes-map-type: atomic
                                              secretKeyRef:
                                                description: Selects a key of a secret
                                                  in the pod's namespace
                                                properties:
                                                  key:
                                                    description: The key of the secret
                                                      to select from.  Must be a valid
                                                      secret key.
                                                    type: string
                                                  name:
                                                    default: ""
                                                    description: |-
                                                      Name of the referent.
                                                      This field is effectively required, but due to backwards compatibility is
                                                      allowed to be empty. Instances of this type with an empty value here are
                                                      almost certainly wrong.
                                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                                    type: string
                                                  optional:
                                                    description: Specify whether the
                                                      Secret or its key must be defined
                                                    type: boolean
                                                required:
                                                - key
                                                type: object
                                                x-kubernetes-map-type: atomic
                                            type: object
                                        required:
                                        - name
                                        type: object
                                      maxItems: 16
                                      type: array
                                    refEnv:
                                      description: |-
                                        RefEnv, when set, is the game-image env the operator points at the
                                        chosen modpack reference (slug/URL) — e.g. "MODRINTH_MODPACK" for the
                                        itzg image. Installing then patches GameServer.spec.env and restarts;
                                        one modpack is active per server. When empty, installing instead
                                        resolves and installs the modpack's dependency mods (e.g. a
                                        Thunderstore/BepInEx pack).
                                      maxLength: 64
                                      type: string
                                  type: object
                                provider:
                                  description: |-
                                    Provider names the built-in registry engine: "modrinth" (Minecraft
                                    mods/plugins, keyless), "thunderstore" (BepInEx games, keyless,
                                    per-community), "curseforge" (Minecraft mods/modpacks, needs an API
                                    key), or "hangar" (PaperMC plugins, keyless).
                                  enum:
                                  - modrinth
                                  - thunderstore
                                  - curseforge
                                  - hangar
                                  type: string
                              required:
                              - provider
                              type: object
                            maxItems: 8
                            minItems: 1
                            type: array
                        required:
                        - providers
                        type: object
                    type: object
                    x-kubernetes-validations:
                    - message: mods requires either path or at least one loaders entry
                      rule: has(self.path) || (has(self.loaders) && size(self.loaders)
                        > 0)
                  players:
                    description: Players configures the moderation actions on the
                      Players tab.
                    properties:
                      ban:
                        description: Ban bans a player.
                        type: string
                      banList:
                        description: BanList configures reading the current ban list.
                        properties:
                          command:
                            description: Command prints the ban list (e.g. "banlist
                              players").
                            minLength: 1
                            type: string
                          entryRegex:
                            description: |-
                              EntryRegex matches one banned player per output line, using the
                              named groups "name" (required), "source" and "reason" (optional).
                            minLength: 1
                            type: string
                        required:
                        - command
                        - entryRegex
                        type: object
                      kick:
                        description: |-
                          Kick disconnects a player, e.g.
                          "kick {{.Player}}{{if .Reason}} {{.Reason}}{{end}}".
                        type: string
                      unban:
                        description: Unban lifts a ban, e.g. "pardon {{.Player}}".
                        type: string
                      whitelist:
                        description: |-
                          Whitelist configures the allow-list management actions on the
                          Players tab (list / add / remove).
                        properties:
                          add:
                            description: Add adds a player, e.g. "whitelist add {{.Player}}".
                            minLength: 1
                            type: string
                          list:
                            description: List prints the current whitelist (e.g. "whitelist
                              list").
                            minLength: 1
                            type: string
                          listRegex:
                            description: |-
                              ListRegex extracts whitelisted names from the List output. If it has
                              a named group "names", that group is split on commas (Minecraft's
                              "There are N whitelisted players: a, b, c"); otherwise each line's
                              "name" group is one entry.
                            minLength: 1
                            type: string
                          remove:
                            description: Remove removes a player, e.g. "whitelist
                              remove {{.Player}}".
                            minLength: 1
                            type: string
                        required:
                        - add
                        - list
                        - listRegex
                        - remove
                        type: object
                    type: object
                  quiesce:
                    description: |-
                      Quiesce configures how in-flight game state is flushed to disk
                      before a backup snapshot (and resumed afterwards).
                    properties:
                      failurePattern:
                        description: |-
                          FailurePattern, when it matches a quiesce command's output
                          (case-insensitive regex), treats the step as failed even though
                          the command itself returned successfully.
                        type: string
                      quiesce:
                        description: |-
                          Quiesce runs before the snapshot, in order (e.g. ["save-off",
                          "save-all flush"]). Any command error triggers a best-effort
                          Unquiesce.
                        items:
                          type: string
                        minItems: 1
                        type: array
                      unquiesce:
                        description: Unquiesce runs after the snapshot (e.g. ["save-on"]).
                        items:
                          type: string
                        minItems: 1
                        type: array
                    required:
                    - quiesce
                    - unquiesce
                    type: object
                  status:
                    description: |-
                      Status declares game-specific live metrics shown on the Overview
                      tab, each read via an RCON command parsed by a named-group regex.
                      Like actions, they require rcon.protocol != none.
                    properties:
                      metrics:
                        description: |-
                          Metrics are the per-game readouts. Each runs an RCON command and
                          extracts a value via a named-group regex.
                        items:
                          description: StatusMetricSpec reads one live metric from
                            an RCON command's output.
                          properties:
                            command:
                              description: Command is the RCON command whose output
                                is parsed.
                              minLength: 1
                              type: string
                            displayName:
                              description: DisplayName labels the metric in the UI.
                              minLength: 1
                              type: string
                            id:
                              description: ID is a stable identifier, unique within
                                the template's metrics.
                              maxLength: 63
                              pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                              type: string
                            regex:
                              description: |-
                                Regex extracts the value via the named group "value", e.g.
                                `TPS: (?P<value>[0-9.]+)`.
                              minLength: 1
                              type: string
                            unit:
                              description: Unit is an optional suffix shown after
                                the value (e.g. "ms", "TPS").
                              type: string
                          required:
                          - command
                          - displayName
                          - id
                          - regex
                          type: object
                        maxItems: 16
                        type: array
                    type: object
                type: object
              command:
                description: Command / Args override the container image entrypoint
                  when set.
                items:
                  type: string
                type: array
              configFiles:
                description: |-
                  ConfigFiles declares files the operator renders from the resolved
                  config values and places under Storage.MountPath before the game
                  starts. Rendered contents are stored in an owned `<server>-files`
                  Secret (they may embed password values) and copied onto the data
                  volume by an init container on every pod start — manual edits to
                  these paths via the Files tab are overwritten on restart.
                items:
                  description: |-
                    ConfigFile is a single operator-rendered file on the game's data
                    volume.
                  properties:
                    path:
                      description: |-
                        Path is where the rendered file lands, relative to
                        Storage.MountPath (e.g. "serverconfig.txt", "cfg/server.cfg").
                        Absolute paths and ".." segments are rejected.
                      maxLength: 512
                      minLength: 1
                      type: string
                      x-kubernetes-validations:
                      - message: path must be relative to the data mount and must
                          not contain '..'
                        rule: '!self.startsWith(''/'') && !self.contains(''..'')'
                    template:
                      description: |-
                        Template is a Go text/template rendered with `.Values` (every
                        configSchema field name mapped to its resolved value, "" when an
                        optional field is unset) and `.Server` (`.Name`, `.Namespace`).
                        Rendering uses missingkey=error: referencing a key outside the
                        schema fails the GameServer.
                      minLength: 1
                      type: string
                  required:
                  - path
                  - template
                  type: object
                maxItems: 32
                type: array
              configSchema:
                description: |-
                  ConfigSchema declares user-tunable fields surfaced in the Create
                  Server wizard. The operator resolves GameServer.spec.config
                  against this schema (applying defaults, validating types/enums)
                  and sets each resolved value as an env var on the game container.
                  Fields with target=file are consumed by ConfigFiles templates
                  instead of becoming env vars.
                items:
                  description: ConfigField is a single user-tunable setting surfaced
                    in the wizard.
                  properties:
                    default:
                      description: Default is the default value (as a string) rendered
                        in the wizard.
                      type: string
                    description:
                      description: Description explains the field to end users.
                      type: string
                    displayName:
                      description: DisplayName is shown in the UI.
                      type: string
                    enum:
                      description: Enum restricts valid values when Type=enum.
                      items:
                        type: string
                      type: array
                    name:
                      description: |-
                        Name is the field identifier (also used as an env var when
                        Target is "env").
                      minLength: 1
                      type: string
                    required:
                      description: Required, when true, blocks wizard submission if
                        unset.
                      type: boolean
                    target:
                      default: env
                      description: |-
                        Target controls where the value is applied. "env" (default) sets
                        an env var on the game container; "file" makes the value available
                        to spec.configFiles templates instead of the environment.
                      enum:
                      - env
                      - file
                      type: string
                    type:
                      default: string
                      description: |-
                        Type controls the input widget in the wizard. Values of password
                        fields are stored in a per-GameServer Secret and injected via
                        SecretKeyRef, never inline in the pod spec.
                      enum:
                      - string
                      - int
                      - bool
                      - enum
                      - password
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              consoleMode:
                description: |-
                  ConsoleMode controls how the dashboard's Console tab attaches to
                  the running game.

                    - "rcon": send line-based RCON commands (default when RCON is set
                      and its protocol is not "none").
                    - "pty":  attach to the game container's stdin/stdout via the
                      Kubernetes pod-attach API. Requires the container to be started
                      with tty=true and stdin=true (the operator sets these). Switching
                      to pty after the pod exists requires a pod recreate.
                    - "none": disable the Console tab for this game.

                  When unset, the operator defaults to "rcon" if RCON.Protocol is set
                  to anything other than "none", and "none" otherwise.
                enum:
                - rcon
                - pty
                - none
                type: string
              description:
                description: Description is free-form markdown describing the template.
                type: string
              displayName:
                description: DisplayName is a human-friendly label shown in the dashboard.
                minLength: 1
                type: string
              env:
                description: Env is the default environment for the game container.
                items:
                  description: EnvVar represents an environment variable present in
                    a Container.
                  properties:
                    name:
                      description: |-
                        Name of the environment variable.
                        May consist of any printable ASCII characters except '='.
                      type: string
                    value:
                      description: |-
                        Variable references $(VAR_NAME) are expanded
                        using the previously defined environment variables in the container and
                        any service environment variables. If a variable cannot be resolved,
                        the reference in the input string will be unchanged. Double $$ are reduced
                        to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                        "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                        Escaped references will never be expanded, regardless of whether the variable
                        exists or not.
                        Defaults to "".
                      type: string
                    valueFrom:
                      description: Source for the environment variable's value. Cannot
                        be used if value is not empty.
                      properties:
                        configMapKeyRef:
                          description: Selects a key of a ConfigMap.
                          properties:
                            key:
                              description: The key to select.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the ConfigMap or its key
                                must be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                        fieldRef:
                          description: |-
                            Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                            spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                          properties:
                            apiVersion:
                              description: Version of the schema the FieldPath is
                                written in terms of, defaults to "v1".
                              type: string
                            fieldPath:
                              description: Path of the field to select in the specified
                                API version.
                              type: string
                          required:
                          - fieldPath
                          type: object
                          x-kubernetes-map-type: atomic
                        fileKeyRef:
                          description: |-
                            FileKeyRef selects a key of the env file.
                            Requires the EnvFiles feature gate to be enabled.
                          properties:
                            key:
                              description: |-
                                The key within the env file. An invalid key will prevent the pod from starting.
                                The keys defined within a source may consist of any printable ASCII characters except '='.
                                During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                              type: string
                            optional:
                              default: false
                              description: |-
                                Specify whether the file or its key must be defined. If the file or key
                                does not exist, then the env var is not published.
                                If optional is set to true and the specified key does not exist,
                                the environment variable will not be set in the Pod's containers.

                                If optional is set to false and the specified key does not exist,
                                an error will be returned during Pod creation.
                              type: boolean
                            path:
                              description: |-
                                The path within the volume from which to select the file.
                                Must be relative and may not contain the '..' path or start with '..'.
                              type: string
                            volumeName:
                              description: The name of the volume mount containing
                                the env file.
                              type: string
                          required:
                          - key
                          - path
                          - volumeName
                          type: object
                          x-kubernetes-map-type: atomic
                        resourceFieldRef:
                          description: |-
                            Selects a resource of the container: only resources limits and requests
                            (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                          properties:
                            containerName:
                              description: 'Container name: required for volumes,
                                optional for env vars'
                              type: string
                            divisor:
                              anyOf:
                              - type: integer
                              - type: string
                              description: Specifies the output format of the exposed
                                resources, defaults to "1"
                              pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                              x-kubernetes-int-or-string: true
                            resource:
                              description: 'Required: resource to select'
                              type: string
                          required:
                          - resource
                          type: object
                          x-kubernetes-map-type: atomic
                        secretKeyRef:
                          description: Selects a key of a secret in the pod's namespace
                          properties:
                            key:
                              description: The key of the secret to select from.  Must
                                be a valid secret key.
                              type: string
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                              type: string
                            optional:
                              description: Specify whether the Secret or its key must
                                be defined
                              type: boolean
                          required:
                          - key
                          type: object
                          x-kubernetes-map-type: atomic
                      type: object
                  required:
                  - name
                  type: object
                type: array
              game:
                description: |-
                  Game is the canonical game identifier (e.g. "minecraft-java",
                  "valheim", "factorio"). Used as a grouping key in the UI catalog.
                minLength: 1
                type: string
              icon:
                description: Icon is an optional URL or data URI shown in the catalog.
                type: string
              image:
                description: |-
                  Image is the default container image (e.g.
                  "itzg/minecraft-server:2025.1.0"). Used as the fallback when the
                  server selects no version (see Versions) and sets no spec.image
                  override. Can be overridden by GameServer.
                minLength: 1
                type: string
              logPath:
                description: |-
                  LogPath is the file holding the game's primary log, as seen from
                  inside the pod (e.g. "/data/logs/latest.log"). It must live under
                  the shared data volume (Storage.MountPath) so the agent sidecar
                  can tail it for the dashboard's Logs tab. Leave empty for games
                  that only log to stdout — the Logs tab is then unavailable.
                type: string
              ports:
                description: Ports declares exposed ports on the game container.
                items:
                  description: GamePort is a single exposed port.
                  properties:
                    advertise:
                      default: true
                      description: |-
                        Advertise controls whether this port is exposed to users via
                        GameServer.Spec.Networking. RCON and query ports are typically
                        not advertised publicly.
                      type: boolean
                    containerPort:
                      description: ContainerPort is the port the game listens on inside
                        the pod.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    name:
                      description: Name is a DNS-label port identifier (e.g. "game",
                        "query", "rcon").
                      minLength: 1
                      type: string
                    protocol:
                      default: TCP
                      description: Protocol is one of TCP, UDP. Defaults to TCP.
                      enum:
                      - TCP
                      - UDP
                      type: string
                  required:
                  - containerPort
                  - name
                  type: object
                type: array
              probes:
                description: |-
                  Probes are the default readiness/liveness/startup probes for the
                  game container. Operator supplies sane defaults when unset.
                properties:
                  liveness:
                    description: |-
                      Probe describes a health check to be performed against a container to determine whether it is
                      alive or ready to receive traffic.
                    properties:
                      exec:
                        description: Exec specifies a command to execute in the container.
                        properties:
                          command:
                            description: |-
                              Command is the command line to execute inside the container, the working directory for the
                              command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                              not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                              a shell, you need to explicitly call out to that shell.
                              Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      failureThreshold:
                        description: |-
                          Minimum consecutive failures for the probe to be considered failed after having succeeded.
                          Defaults to 3. Minimum value is 1.
                        format: int32
                        type: integer
                      grpc:
                        description: GRPC specifies a GRPC HealthCheckRequest.
                        properties:
                          port:
                            description: Port number of the gRPC service. Number must
                              be in the range 1 to 65535.
                            format: int32
                            type: integer
                          service:
                            default: ""
                            description: |-
                              Service is the name of the service to place in the gRPC HealthCheckRequest
                              (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                              If this is not specified, the default behavior is defined by gRPC.
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGet specifies an HTTP GET request to perform.
                        properties:
                          host:
                            description: |-
                              Host name to connect to, defaults to the pod IP. You probably want to set
                              "Host" in httpHeaders instead.
                            type: string
                          httpHeaders:
                            description: Custom headers to set in the request. HTTP
                              allows repeated headers.
                            items:
                              description: HTTPHeader describes a custom header to
                                be used in HTTP probes
                              properties:
                                name:
                                  description: |-
                                    The header field name.
                                    This will be canonicalized upon output, so case-variant names will be understood as the same header.
                                  type: string
                                value:
                                  description: The header field value
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          path:
                            description: Path to access on the HTTP server.
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Name or number of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                          scheme:
                            description: |-
                              Scheme to use for connecting to the host.
                              Defaults to HTTP.
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        description: |-
                          Number of seconds after the container has started before liveness probes are initiated.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                      periodSeconds:
                        description: |-
                          How often (in seconds) to perform the probe.
                          Default to 10 seconds. Minimum value is 1.
                        format: int32
                        type: integer
                      successThreshold:
                        description: |-
                          Minimum consecutive successes for the probe to be considered successful after having failed.
                          Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                        format: int32
                        type: integer
                      tcpSocket:
                        description: TCPSocket specifies a connection to a TCP port.
                        properties:
                          host:
                            description: 'Optional: Host name to connect to, defaults
                              to the pod IP.'
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Number or name of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                        required:
                        - port
                        type: object
                      terminationGracePeriodSeconds:
                        description: |-
                          Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                          The grace period is the duration in seconds after the processes running in the pod are sent
                          a termination signal and the time when the processes are forcibly halted with a kill signal.
                          Set this value longer than the expected cleanup time for your process.
                          If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                          value overrides the value provided by the pod spec.
                          Value must be non-negative integer. The value zero indicates stop immediately via
                          the kill signal (no opportunity to shut down).
                          This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                          Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                        format: int64
                        type: integer
                      timeoutSeconds:
                        description: |-
                          Number of seconds after which the probe times out.
                          Defaults to 1 second. Minimum value is 1.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                    type: object
                  readiness:
                    description: |-
                      Probe describes a health check to be performed against a container to determine whether it is
                      alive or ready to receive traffic.
                    properties:
                      exec:
                        description: Exec specifies a command to execute in the container.
                        properties:
                          command:
                            description: |-
                              Command is the command line to execute inside the container, the working directory for the
                              command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                              not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                              a shell, you need to explicitly call out to that shell.
                              Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      failureThreshold:
                        description: |-
                          Minimum consecutive failures for the probe to be considered failed after having succeeded.
                          Defaults to 3. Minimum value is 1.
                        format: int32
                        type: integer
                      grpc:
                        description: GRPC specifies a GRPC HealthCheckRequest.
                        properties:
                          port:
                            description: Port number of the gRPC service. Number must
                              be in the range 1 to 65535.
                            format: int32
                            type: integer
                          service:
                            default: ""
                            description: |-
                              Service is the name of the service to place in the gRPC HealthCheckRequest
                              (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                              If this is not specified, the default behavior is defined by gRPC.
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGet specifies an HTTP GET request to perform.
                        properties:
                          host:
                            description: |-
                              Host name to connect to, defaults to the pod IP. You probably want to set
                              "Host" in httpHeaders instead.
                            type: string
                          httpHeaders:
                            description: Custom headers to set in the request. HTTP
                              allows repeated headers.
                            items:
                              description: HTTPHeader describes a custom header to
                                be used in HTTP probes
                              properties:
                                name:
                                  description: |-
                                    The header field name.
                                    This will be canonicalized upon output, so case-variant names will be understood as the same header.
                                  type: string
                                value:
                                  description: The header field value
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          path:
                            description: Path to access on the HTTP server.
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Name or number of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                          scheme:
                            description: |-
                              Scheme to use for connecting to the host.
                              Defaults to HTTP.
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        description: |-
                          Number of seconds after the container has started before liveness probes are initiated.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                      periodSeconds:
                        description: |-
                          How often (in seconds) to perform the probe.
                          Default to 10 seconds. Minimum value is 1.
                        format: int32
                        type: integer
                      successThreshold:
                        description: |-
                          Minimum consecutive successes for the probe to be considered successful after having failed.
                          Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                        format: int32
                        type: integer
                      tcpSocket:
                        description: TCPSocket specifies a connection to a TCP port.
                        properties:
                          host:
                            description: 'Optional: Host name to connect to, defaults
                              to the pod IP.'
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Number or name of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                        required:
                        - port
                        type: object
                      terminationGracePeriodSeconds:
                        description: |-
                          Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                          The grace period is the duration in seconds after the processes running in the pod are sent
                          a termination signal and the time when the processes are forcibly halted with a kill signal.
                          Set this value longer than the expected cleanup time for your process.
                          If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                          value overrides the value provided by the pod spec.
                          Value must be non-negative integer. The value zero indicates stop immediately via
                          the kill signal (no opportunity to shut down).
                          This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                          Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                        format: int64
                        type: integer
                      timeoutSeconds:
                        description: |-
                          Number of seconds after which the probe times out.
                          Defaults to 1 second. Minimum value is 1.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                    type: object
                  startup:
                    description: |-
                      Probe describes a health check to be performed against a container to determine whether it is
                      alive or ready to receive traffic.
                    properties:
                      exec:
                        description: Exec specifies a command to execute in the container.
                        properties:
                          command:
                            description: |-
                              Command is the command line to execute inside the container, the working directory for the
                              command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                              not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                              a shell, you need to explicitly call out to that shell.
                              Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        type: object
                      failureThreshold:
                        description: |-
                          Minimum consecutive failures for the probe to be considered failed after having succeeded.
                          Defaults to 3. Minimum value is 1.
                        format: int32
                        type: integer
                      grpc:
                        description: GRPC specifies a GRPC HealthCheckRequest.
                        properties:
                          port:
                            description: Port number of the gRPC service. Number must
                              be in the range 1 to 65535.
                            format: int32
                            type: integer
                          service:
                            default: ""
                            description: |-
                              Service is the name of the service to place in the gRPC HealthCheckRequest
                              (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                              If this is not specified, the default behavior is defined by gRPC.
                            type: string
                        required:
                        - port
                        type: object
                      httpGet:
                        description: HTTPGet specifies an HTTP GET request to perform.
                        properties:
                          host:
                            description: |-
                              Host name to connect to, defaults to the pod IP. You probably want to set
                              "Host" in httpHeaders instead.
                            type: string
                          httpHeaders:
                            description: Custom headers to set in the request. HTTP
                              allows repeated headers.
                            items:
                              description: HTTPHeader describes a custom header to
                                be used in HTTP probes
                              properties:
                                name:
                                  description: |-
                                    The header field name.
                                    This will be canonicalized upon output, so case-variant names will be understood as the same header.
                                  type: string
                                value:
                                  description: The header field value
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                            x-kubernetes-list-type: atomic
                          path:
                            description: Path to access on the HTTP server.
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Name or number of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                          scheme:
                            description: |-
                              Scheme to use for connecting to the host.
                              Defaults to HTTP.
                            type: string
                        required:
                        - port
                        type: object
                      initialDelaySeconds:
                        description: |-
                          Number of seconds after the container has started before liveness probes are initiated.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                      periodSeconds:
                        description: |-
                          How often (in seconds) to perform the probe.
                          Default to 10 seconds. Minimum value is 1.
                        format: int32
                        type: integer
                      successThreshold:
                        description: |-
                          Minimum consecutive successes for the probe to be considered successful after having failed.
                          Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                        format: int32
                        type: integer
                      tcpSocket:
                        description: TCPSocket specifies a connection to a TCP port.
                        properties:
                          host:
                            description: 'Optional: Host name to connect to, defaults
                              to the pod IP.'
                            type: string
                          port:
                            anyOf:
                            - type: integer
                            - type: string
                            description: |-
                              Number or name of the port to access on the container.
                              Number must be in the range 1 to 65535.
                              Name must be an IANA_SVC_NAME.
                            x-kubernetes-int-or-string: true
                        required:
                        - port
                        type: object
                      terminationGracePeriodSeconds:
                        description: |-
                          Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                          The grace period is the duration in seconds after the processes running in the pod are sent
                          a termination signal and the time when the processes are forcibly halted with a kill signal.
                          Set this value longer than the expected cleanup time for your process.
                          If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                          value overrides the value provided by the pod spec.
                          Value must be non-negative integer. The value zero indicates stop immediately via
                          the kill signal (no opportunity to shut down).
                          This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                          Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                        format: int64
                        type: integer
                      timeoutSeconds:
                        description: |-
                          Number of seconds after which the probe times out.
                          Defaults to 1 second. Minimum value is 1.
                          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                        format: int32
                        type: integer
                    type: object
                type: object
              rcon:
                description: RCON declares the remote-console protocol used by this
                  game, if any.
                properties:
                  passwordEnv:
                    description: |-
                      PasswordEnv is the environment variable the game container reads
                      the RCON password from (e.g. "RCON_PASSWORD" for itzg/minecraft).
                      When set, the operator injects the resolved password into the game
                      container via this env var and mounts the same value for the agent
                      sidecar, so the dashboard console can authenticate. Leave empty for
                      games that take their RCON password some other way.
                    type: string
                  passwordSecretRef:
                    description: |-
                      PasswordSecretRef references a Secret+key containing the RCON
                      password. If unset, the operator generates a password and stores
                      it in an auto-managed Secret (<gameserver>-rcon, key "password").
                    properties:
                      key:
                        minLength: 1
                        type: string
                      name:
                        minLength: 1
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  port:
                    description: Port is the TCP port RCON listens on inside the pod.
                    format: int32
                    type: integer
                  protocol:
                    default: source
                    description: |-
                      Protocol is the wire protocol. "source" is the Valve/Minecraft RCON
                      protocol; other games use different mechanisms (telnet, HTTP, etc.)
                      and are implemented by module-specific code paths.
                    enum:
                    - source
                    - telnet
                    - http
                    - none
                    type: string
                type: object
              resources:
                description: Resources are the default compute resources for the game
                  container.
                properties:
                  claims:
                    description: |-
                      Claims lists the names of resources, defined in spec.resourceClaims,
                      that are used by this container.

                      This field depends on the
                      DynamicResourceAllocation feature gate.

                      This field is immutable. It can only be set for containers.
                    items:
                      description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                      properties:
                        name:
                          description: |-
                            Name must match the name of one entry in pod.spec.resourceClaims of
                            the Pod where this field is used. It makes that resource available
                            inside a container.
                          type: string
                        request:
                          description: |-
                            Request is the name chosen for a request in the referenced claim.
                            If empty, everything from the claim is made available, otherwise
                            only the result of this request.
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Limits describes the maximum amount of compute resources allowed.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: |-
                      Requests describes the minimum amount of compute resources required.
                      If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                      otherwise to an implementation-defined value. Requests cannot exceed Limits.
                      More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                    type: object
                type: object
              storage:
                description: Storage describes the default persistent storage layout.
                properties:
                  dataSource:
                    description: |-
                      DataSource, when set on a GameServer, seeds the data PVC from an
                      existing CSI VolumeSnapshot the first time the PVC is created. This
                      is how volume-snapshot Restores stand up a new server from a
                      snapshot. It is immutable once the PVC binds and has no effect when
                      set on a GameTemplate (it is a per-server restore concern).
                    properties:
                      kind:
                        default: VolumeSnapshot
                        description: |-
                          Kind is the data-source object kind. Only "VolumeSnapshot" is
                          supported today.
                        enum:
                        - VolumeSnapshot
                        type: string
                      name:
                        description: Name is the VolumeSnapshot object name in the
                          GameServer's namespace.
                        maxLength: 253
                        minLength: 1
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  mountPath:
                    default: /data
                    description: |-
                      MountPath is where the game persistent volume is mounted inside
                      the game container (e.g. "/data").
                    type: string
                  size:
                    anyOf:
                    - type: integer
                    - type: string
                    description: Size is the default PVC size (e.g. "10Gi").
                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                    x-kubernetes-int-or-string: true
                  storageClassName:
                    description: StorageClassName, when set, pins the PVC to a specific
                      StorageClass.
                    type: string
                type: object
              version:
                description: |-
                  Version is the template revision (e.g. "1.0.0"). Bump when changing
                  defaults in ways that existing GameServers should opt into.
                minLength: 1
                type: string
              versions:
                description: |-
                  Versions is an optional catalog of selectable game versions surfaced
                  in the Create Server wizard. Each entry maps a user choice to a
                  concrete image (and optional per-version env / mod loader). When empty
                  there is no version choice: a server runs spec.image (override) or this
                  template's Image, exactly as before. At most one entry should set
                  default=true; otherwise the first entry is the wizard's default.
                items:
                  description: |-
                    GameVersion is one selectable entry in a template's version catalog.
                    Selecting it (via GameServer.spec.version) pins the container image,
                    appends this entry's env, and — when Loader names a capabilities.mods
                    loader — provisions and mounts that loader's per-(version+loader) mod
                    volume.
                  properties:
                    default:
                      description: |-
                        Default marks the wizard's pre-selected entry. At most one entry should
                        set it; if none (or several) do, the first entry is the default.
                      type: boolean
                    displayName:
                      description: |-
                        DisplayName labels the entry in the version picker, e.g.
                        "1.21.4 (Paper)".
                      maxLength: 128
                      minLength: 1
                      type: string
                    env:
                      description: |-
                        Env is appended when this version is selected (e.g. TYPE=PAPER,
                        VERSION=1.21.4 for itzg). It is applied after the template's Env and
                        before GameServer.spec.env, so an explicit user override still wins.
                      items:
                        description: EnvVar represents an environment variable present
                          in a Container.
                        properties:
                          name:
                            description: |-
                              Name of the environment variable.
                              May consist of any printable ASCII characters except '='.
                            type: string
                          value:
                            description: |-
                              Variable references $(VAR_NAME) are expanded
                              using the previously defined environment variables in the container and
                              any service environment variables. If a variable cannot be resolved,
                              the reference in the input string will be unchanged. Double $$ are reduced
                              to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                              "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                              Escaped references will never be expanded, regardless of whether the variable
                              exists or not.
                              Defaults to "".
                            type: string
                          valueFrom:
                            description: Source for the environment variable's value.
                              Cannot be used if value is not empty.
                            properties:
                              configMapKeyRef:
                                description: Selects a key of a ConfigMap.
                                properties:
                                  key:
                                    description: The key to select.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the ConfigMap or
                                      its key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                              fieldRef:
                                description: |-
                                  Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                  spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                properties:
                                  apiVersion:
                                    description: Version of the schema the FieldPath
                                      is written in terms of, defaults to "v1".
                                    type: string
                                  fieldPath:
                                    description: Path of the field to select in the
                                      specified API version.
                                    type: string
                                required:
                                - fieldPath
                                type: object
                                x-kubernetes-map-type: atomic
                              fileKeyRef:
                                description: |-
                                  FileKeyRef selects a key of the env file.
                                  Requires the EnvFiles feature gate to be enabled.
                                properties:
                                  key:
                                    description: |-
                                      The key within the env file. An invalid key will prevent the pod from starting.
                                      The keys defined within a source may consist of any printable ASCII characters except '='.
                                      During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                    type: string
                                  optional:
                                    default: false
                                    description: |-
                                      Specify whether the file or its key must be defined. If the file or key
                                      does not exist, then the env var is not published.
                                      If optional is set to true and the specified key does not exist,
                                      the environment variable will not be set in the Pod's containers.

                                      If optional is set to false and the specified key does not exist,
                                      an error will be returned during Pod creation.
                                    type: boolean
                                  path:
                                    description: |-
                                      The path within the volume from which to select the file.
                                      Must be relative and may not contain the '..' path or start with '..'.
                                    type: string
                                  volumeName:
                                    description: The name of the volume mount containing
                                      the env file.
                                    type: string
                                required:
                                - key
                                - path
                                - volumeName
                                type: object
                                x-kubernetes-map-type: atomic
                              resourceFieldRef:
                                description: |-
                                  Selects a resource of the container: only resources limits and requests
                                  (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                properties:
                                  containerName:
                                    description: 'Container name: required for volumes,
                                      optional for env vars'
                                    type: string
                                  divisor:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: Specifies the output format of the
                                      exposed resources, defaults to "1"
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                  resource:
                                    description: 'Required: resource to select'
                                    type: string
                                required:
                                - resource
                                type: object
                                x-kubernetes-map-type: atomic
                              secretKeyRef:
                                description: Selects a key of a secret in the pod's
                                  namespace
                                properties:
                                  key:
                                    description: The key of the secret to select from.  Must
                                      be a valid secret key.
                                    type: string
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                  optional:
                                    description: Specify whether the Secret or its
                                      key must be defined
                                    type: boolean
                                required:
                                - key
                                type: object
                                x-kubernetes-map-type: atomic
                            type: object
                        required:
                        - name
                        type: object
                      type: array
                    gameVersion:
                      description: |-
                        GameVersion is the upstream game-version token the dashboard passes to
                        an external mod registry to filter results to this version (e.g.
                        Modrinth's "game_versions" facet), e.g. "1.21.4". It is distinct from
                        ID (a Kestrel selector like "1.21.4-paper" that's too lossy to parse
                        reliably). Optional; when unset the registry search sends no version
                        facet and returns mods for all versions. Ignored by registries with no
                        version dimension (e.g. Thunderstore) and when the template declares no
                        mods.registry.
                      maxLength: 32
                      type: string
                    id:
                      description: |-
                        ID is the stable selector stored in GameServer.spec.version. It is
                        also folded into the per-version mod volume/PVC names, so keep it short
                        and DNS-ish (dots and hyphens allowed; sanitized to a DNS label for
                        volume names). E.g. "1.21.4-paper", "tmodloader-latest".
                      maxLength: 40
                      pattern: ^[a-z0-9]([a-z0-9.-]*[a-z0-9])?$
                      type: string
                    image:
                      description: |-
                        Image is the full container reference for this version. For
                        env-versioned games (e.g. itzg/minecraft-server, which picks software
                        via TYPE/VERSION env) this is usually one pinned image shared across
                        entries, differentiated only by Loader/Env. For tag-versioned games
                        (e.g. Terraria) each entry pins a distinct tag.
                      minLength: 1
                      type: string
                    loader:
                      description: |-
                        Loader is the mod-loader / server-type key (e.g. "paper", "forge",
                        "fabric", "vanilla", "bepinex", "tmodloader"). It keys into
                        capabilities.mods.loaders to select this combo's mod volume. Empty
                        means this version has no loader dimension (mods, if any, live on the
                        shared data volume).
                      maxLength: 40
                      pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                      type: string
                  required:
                  - displayName
                  - id
                  - image
                  type: object
                maxItems: 64
                type: array
            required:
            - displayName
            - game
            - image
            - version
            type: object
            x-kubernetes-validations:
            - message: consoleMode 'rcon' requires spec.rcon with a protocol other
                than 'none'
              rule: '!has(self.consoleMode) || self.consoleMode != ''rcon'' || (has(self.rcon)
                && (!has(self.rcon.protocol) || self.rcon.protocol != ''none''))'
          status:
            description: |-
              GameTemplateStatus is intentionally minimal — templates are mostly
              static configuration. The operator populates ObservedGeneration so
              controllers reading templates can detect spec updates.
            properties:
              inUseCount:
                description: |-
                  InUseCount is the number of GameServers currently referencing
                  this template.
                format: int32
                type: integer
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
