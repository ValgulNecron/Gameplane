apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: restores.kestrel.gg
spec:
  group: kestrel.gg
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    singular: restore
    shortNames:
      - rs
  scope: Namespaced
  versions:
    - name: v1alpha1
      served: true
      storage: true
      additionalPrinterColumns:
        - name: Backup
          type: string
          jsonPath: .spec.backupRef.name
        - name: Server
          type: string
          jsonPath: .spec.serverRef.name
        - name: Phase
          type: string
          jsonPath: .status.phase
        - name: Completed
          type: date
          jsonPath: .status.completionTime
      subresources:
        status: {}
      schema:
        openAPIV3Schema:
          type: object
          properties:
            apiVersion: { type: string }
            kind: { type: string }
            metadata: { type: object }
            spec:
              type: object
              required: [backupRef, serverRef]
              properties:
                backupRef:
                  type: object
                  required: [name]
                  properties:
                    name: { type: string, minLength: 1 }
                serverRef:
                  type: object
                  required: [name]
                  properties:
                    name: { type: string, minLength: 1 }
            status:
              type: object
              properties:
                phase:
                  type: string
                  enum: [Pending, Suspending, Running, Resuming, Succeeded, Failed]
                observedGeneration: { type: integer, format: int64 }
                snapshotID: { type: string }
                startTime: { type: string, format: date-time }
                completionTime: { type: string, format: date-time }
                message: { type: string }
                conditions:
                  type: array
                  items:
                    type: object
                    required: [type, status, lastTransitionTime, reason, message]
                    properties:
                      type: { type: string }
                      status: { type: string }
                      lastTransitionTime: { type: string, format: date-time }
                      reason: { type: string }
                      message: { type: string }
                      observedGeneration: { type: integer, format: int64 }
