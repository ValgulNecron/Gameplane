---
description: "Task list for Feature 015: Dedicated Server Modules for Top Steam Games"
---

# Tasks: Dedicated Server Modules for Top Steam Games

**Input**: Design documents from `specs/015-top-steam-game-modules/`: `spec.md`, `plan.md`, `data-model.md`, `contracts/module-spec-contract.md`, `contracts/gametemplate-contract.md`, `contracts/engine-matrix-contract.md`.

**Prerequisites**: plan.md (required), spec.md (required for user stories), data-model.md, contracts/.

**Branch note**: the git branch checked out for this work is `claude/top-steam-game-modules-x7oyy1`, which does not match either the folder name or (until Phase 2 lands) the in-file headers. The maintainer has ruled the feature keeps its folder number, `015` (see T006-T010): `spec.md`, `plan.md`, `data-model.md`, `research.md`, `quickstart.md`, and all three `contracts/` files are rewritten from `014-top-steam-game-modules` to `015-top-steam-game-modules` by the tasks below, and T131 verifies zero `014-top-steam-game-modules` references remain repo-wide. The branch name itself is unaffected — no task renames it.

**Ground truth**: every task below is written against the shipped CRD schema (`modules/.schema/gametemplate.schema.json`) and the real `modules/` submodule contents. Where `contracts/gametemplate-contract.md` and `data-model.md` previously disagreed with the schema, the maintainer has ruled on every disagreement: T006-T010 correct those artifacts directly (no more "requires maintainer sign-off" deferral — the ruling IS the sign-off), and T005 records the handful of questions that remain genuinely open in a new `specs/015-top-steam-game-modules/OPEN-DECISIONS.md`. In particular: `spec.capabilities.lifecycle.stop` (not `spec.lifecycle.stop`) is an array of 1-16 plain command strings with no `action`/`command`/`timeoutSeconds` keys; `spec.storage` has no `defaultSize`/`subPaths` (real fields: `size`, `mountPath`, `storageClassName`, `dataSource`, `extra`); `spec.security` has no `readOnlyRootFilesystem` (real fields: `runAsUser`, `runAsGroup`, `fsGroup`); the sample manifest is `samples/gameserver.yaml`, not `samples/server.yaml`; `spec.rcon.protocol` is `source|telnet|websocket|battleye|satisfactory|palworld|nuclearoption|none` today, and the maintainer has additionally ruled that `rest` and `cli` become first-class enum values as part of this feature (T016-T025) — this is additive, not breaking. `modules/validate.py:129`'s `RCON_PROTOCOLS` tuple and the generated `modules/.schema/gametemplate.schema.json` enum are kept in sync with the Go CRD by T022 and T017 respectively; T017 also closes the pre-existing drift where the generated schema's enum was missing `nuclearoption` (already present in the Go CRD) — a regeneration side effect, not a hand-edit. The schema's field description states `capabilities.lifecycle.stop` requires `rcon.protocol != none`, but the apiserver does not enforce that as a CEL rule and `modules/dont-starve-together/template.yaml` ships a `stop` sequence under `rcon.protocol: none` + `consoleMode: pty` — this ambiguity is NOT resolved by any maintainer ruling and stays recorded in `OPEN-DECISIONS.md` (T005); see T064.

**Tests**: Included — mandatory per spec.md's User Story acceptance scenarios, FR-011, and Constitution Principle I (E2E-Tested Delivery). Every module gets a real wire-protocol e2e probe in `test/e2e/`; resource-heavy games (>8 GB RAM) get their test committed with an explicit CI-exclusion comment in `test/e2e/buckets.sh`, per the existing `bucket_bot_heavy` pattern — never a silent skip. Every new game additionally gets a `test/e2e/internal/<game>/` probe package (Phase 2) proven to fail against a dead address and succeed against a real listener before any test task is allowed to depend on it, per Constitution Principle I. The two new agent-side RCON clients (`rest.go`, `cli.go` — T018/T019) each need real Go unit tests of their own: `agent/.testcoverage.yml` gates the `agent` module at 90%, and these are new files with no coverage yet. The fail-dead/succeed-live proof is a `go run ./test/e2e/internal/<game>` invocation (a build-and-run, not a test suite) or a CI e2e-bucket run; the Go unit tests for T018/T019 are authored locally and verified only by the CI run in T123 — never by a local `go test` (rule 8).

**Local execution**: Per CLAUDE.md rule 8, `make test`, `make lint`, `make cover`, `go test`, and `npm test` MUST NOT be run locally — GitHub Actions is the source of truth. `python3 modules/validate.py` in its default (non-`--pin`) mode is sanctioned (plan.md: "local execution limited to standard static preflight checks"); T120 uses it, `--pin` mode (T119) rewrites templates and is used only there. CLAUDE.md rule 7 additionally requires `make generate` and `make manifests` to run locally in the same commit as any CRD Go type edit (T016), and `make module-schema` (T017) is the codegen step that keeps the module schema in sync — none of these three are test/lint suites, so running them locally is required, not merely permitted.

**Organization**: Tasks are grouped by user story (US1-US5, priorities from spec.md: US1 P1, US2 P1, US3 P2, US4 P2, US5 P3) so each story is independently deliverable. Because every story after US1 edits the same per-module `template.yaml` files US1 authors, US2-US5 run *after* US1 completes rather than concurrently with it (see Dependencies below) — they remain independently testable, just not independently schedulable against a not-yet-authored file. Phase 2 (Foundational) is now substantially larger than a typical feature's setup phase: besides the per-module prep work, it implements two new `rcon.protocol` values end-to-end (CRD → codegen → agent → validator) and builds four Gameplane-owned container images, so it touches `operator/`, `agent/`, `charts/`, and `web/` in addition to `modules/`, `specs/`, and `test/e2e/`.

## Format: `T### [P?] [US#?] Description`

- **[P]**: Different files, no dependency on another incomplete task in this list — safe to run concurrently with every other `[P]` task in the file, not just its neighbors.
- **[US#]**: User story tag (US1-US5); present on every task inside a user-story phase (3-7) and on no task outside one.
- Every description carries a repo-relative path (never `/home/user/Gameplane/...`).

---

## Phase 1: Setup (Shared Infrastructure)

**Purpose**: Get the `modules/` submodule and its validator ready to receive work.

- [ ] T001 Initialize the `modules/` git submodule (`git submodule update --init modules`) and confirm the checkout is non-empty — `modules/.schema/gametemplate.schema.json`, `modules/.schema/module.schema.json`, and `modules/validate.py` must all be present and readable before any later task runs.
- [ ] T002 [P] Inside the `modules/` submodule checkout (the separate `ValgulNecron/gameplane-module` repo), create a working branch (e.g. `top-steam-game-modules`) off its default branch — `modules/` — since every module content change in this feature is committed in that repo, never in this one; this repo only ever gets the bumped submodule pointer (T125).
- [ ] T003 [P] Confirm `modules/.schema/module.schema.json` and `modules/.schema/gametemplate.schema.json` are the sole binding authority for `module.yaml`/`template.yaml` shape for this feature, superseding any conflicting prose in `specs/015-top-steam-game-modules/data-model.md` and `specs/015-top-steam-game-modules/contracts/gametemplate-contract.md` — anchor file `modules/.schema/gametemplate.schema.json`.
- [ ] T004 [P] Install PyYAML so `python3 modules/validate.py` can run locally (`pip install --user pyyaml`, or a repo-local venv) — `modules/validate.py`.

**Checkpoint**: submodule present, schema authority confirmed, validator runnable locally.

---

## Phase 2: Foundational (Blocking Prerequisites)

**Purpose**: Produce the shared artifacts every Phase 3-7 task consumes: the maintainer-authorized artifact corrections, a real wire-protocol probe package for every new game, two new agent-side RCON protocols (`rest`, `cli`) implemented end-to-end, and the four Gameplane-owned container images the auxiliary-service games need. **Nothing in Phase 3 onward may start until this phase is done**, and no Phase 4-7 test task may depend on a probe package until its proof task (bundled into the same task below) passes.

### 2.1 Open questions and direct artifact corrections

- [ ] T005 [P] Create `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` recording only the questions the maintainer has explicitly left open (everything else this task previously would have deferred — field-shape mismatches, sample-manifest naming, the garrys-mod/7-days-to-die engine-matrix rows, the feature-numbering mismatch, and data-model.md's Required column — is maintainer-authorized and corrected directly by T006-T010, not recorded as a question here): (1) **`cli` rcon.protocol semantics** — the CRD's `capabilities.actions[].transport` field already resolves to rcon when `rcon.protocol != none`, else stdin (pod-attach), and `consoleMode: pty` already attaches the dashboard console to stdin; state explicitly what `cli` means relative to that existing path (does it replace pod-attach, or drive it from the agent instead?), with a recommendation, and state what `cli` implies for `templateHasRCON`/`resolveRCON` (`operator/internal/controller/helpers.go:94`, `gameserver_rcon.go:41`) — is a `<gs>-rcon` Secret minted for a passwordless stdin console? — and for `web/src/lib/capabilities.ts:20`'s `rconAvailable` — T019 depends on this being answered before it writes `cli.go`; (2) a per-module finding for **`factorio`** (ships `rcon.protocol: source` **with** `consoleMode: pty`; `contracts/engine-matrix-contract.md` claims `none`) investigating whether the shipped template or the contract is the bug, with its own recommendation; (3) the same for **`project-zomboid`** (ships `rcon.protocol: source`; the contract claims stdin-only) — do not change either template pending that ruling; (4) the pre-existing, unresolved ambiguity that `modules/dont-starve-together/template.yaml` ships a working `capabilities.lifecycle.stop` array under `rcon.protocol: none`, contradicting the schema's (non-CEL-enforced) field-description prose that the field requires `rcon.protocol != none` — record it, do not resolve it.
- [ ] T006 [P] Fix `specs/015-top-steam-game-modules/contracts/gametemplate-contract.md`: rewrite §3 (Lifecycle Stop Action Contract) to describe `spec.capabilities.lifecycle.stop` as a 1-16-item array of plain command strings (delete the `action`/`command`/`timeoutSeconds` object shape and the fabricated YAML example built around it); correct §2's `rest`/`cli` table row — both become real `rcon.protocol` enum values once T016/T020 land, but `rest`'s "Supported Games" column is wrong (Palworld and Satisfactory keep their own dedicated `palworld`/`satisfactory` protocols per the CRD doc comment, not a generic `rest`) and `cli`'s row must note its game mapping and exact semantics are open pending `OPEN-DECISIONS.md` (T005) — do not assign `cli` to any game in this table; rewrite the `**Feature**:` header from `014-top-steam-game-modules` to `015-top-steam-game-modules`.
- [ ] T007 [P] Fix `specs/015-top-steam-game-modules/data-model.md`: in the §1 mermaid class diagram, correct `StorageDefinition` (drop `defaultSize`/`subPaths`, add `size`/`storageClassName`/`dataSource`/`extra`), `SecurityDefinition` (drop `readOnlyRootFilesystem`, add `runAsGroup`), and `LifecycleDefinition`/`StopAction` (a single `stop: string[]` field on `spec.capabilities.lifecycle`, not an `action`/`command`/`timeoutSeconds` object); in §2.1, drop the Required mark from `module.yaml`'s `categories`, `homepage`, `license`, `gameplaneMinVersion` (`module.schema.json` requires only `apiVersion`/`name`/`displayName`/`version`/`game`/`summary`); in §2.2, delete the `storage.defaultSize`/`storage.subPaths`/`security.readOnlyRootFilesystem` rows, correct the storage/security field lists to match the CRD, rename `spec.lifecycle` to `spec.capabilities.lifecycle`, and drop the Required mark from `storage`, `security`, `lifecycle`, `versions`, `ports`, `categories`, `accentColor`, `description` (the CRD requires only `displayName`/`game`/`image`/`version` — leave those four Required, do not add a validator rule enforcing the rest); in §3.2, delete invariant 4's "timeout of at least 30 seconds" clause (unimplementable against a plain string array) and restate it as: when the engine supports persistence, `capabilities.lifecycle.stop` must contain a valid world-save command sequence. Rewrite the `**Feature**:` header to `015-top-steam-game-modules`.
- [ ] T008 [P] Fix sample-manifest naming and the `015` header across three files: `specs/015-top-steam-game-modules/contracts/module-spec-contract.md` §1's directory-layout tree (`samples/server.yaml` → `samples/gameserver.yaml`, matching every real module — no existing module's sample file is renamed) and its own `**Feature**:` header (line 3); `specs/015-top-steam-game-modules/plan.md`'s Source Code project-structure tree (same rename) and its `**Branch**:`/Summary header at lines 3, 5 and 57 (`014-top-steam-game-modules` → `015-top-steam-game-modules`, and the Summary sentence's `samples/server.yaml` mention); `specs/015-top-steam-game-modules/quickstart.md` Scenario 4's `kubectl apply -f modules/team-fortress-2/samples/server.yaml` example (→ `gameserver.yaml`) and its `**Feature**:` header.
- [ ] T009 [P] Fix `specs/015-top-steam-game-modules/contracts/engine-matrix-contract.md`: correct the `garrys-mod` row to reflect its shipped `rcon.protocol: none` with no `capabilities` block at all (not `source`), quoting `modules/garrys-mod/template.yaml`'s header comment's reason verbatim (no console reachable: baked `cfg/`, no password env, agent rcon-port wiring unfixed); correct the `7-days-to-die` row the same way (not `telnet`), quoting its header comment's reason (TelnetPassword lives in `serverconfig.xml` under `serverfiles/`, unreachable from the world-saves mount). Leave the `factorio` and `project-zomboid` rows untouched — those are open findings tracked in `OPEN-DECISIONS.md` (T005), not settled here. Rewrite the `**Feature**:` header to `015-top-steam-game-modules`.
- [ ] T010 [P] Rewrite `specs/015-top-steam-game-modules/spec.md`'s `**Feature Branch**:` header from `014-top-steam-game-modules` to `015-top-steam-game-modules`. Also verify `specs/015-top-steam-game-modules/checklists/requirements.md` — it links to `spec.md` by relative path and carries no hardcoded feature number today, so it needs no edit; state that explicitly rather than silently skipping the file.
- [ ] T011 [P] Produce `specs/015-top-steam-game-modules/engine-matrix-resolved.md`: a per-module table for all 26 modules in FR-001, giving each one's real `rcon.protocol` (drawn from `source|telnet|websocket|battleye|satisfactory|palworld|nuclearoption|rest|cli|none` — `rest`/`cli` per T016/T020, corrected per T009 for garrys-mod/7-days-to-die), `consoleMode` (`rcon|pty|none`), `spec.ports` entries, `spec.storage.mountPath`, and `spec.capabilities.lifecycle.stop` command array. For `factorio` and `project-zomboid`, use each module's **currently shipped** value (`source` for both, per the real templates) and note plainly that whether this or `contracts/engine-matrix-contract.md`'s claim is correct is still under investigation in `OPEN-DECISIONS.md` (T005) — do not silently resolve it here. No module in this table is assigned `cli`: its game mapping is exactly the open question T005 records. Also derive, from each new module's declared (or to-be-declared) `spec.storage.size`/`storage.extra` and `spec.resources` against the same disk/memory criteria `test/e2e/buckets.sh`'s `bucket_bot_heavy` comment block already uses, a fast/heavy CI-bucket classification column for the 13 new modules; this column is the single source T075/T082/T095/T112 cite instead of asserting a bucket inline. This file is the single input every Phase 3 module task and every Phase 4-7 audit task cites for its rcon/lifecycle/storage/probe/bucket values. The default classification for a SteamCMD-installed or >5Gi-storage game is heavy; justify per module in the column, and note that `fastGameSet` (`gamebot_helpers_e2e_test.go:29`) and the CI bucket are two separate axes. Where a shipped `storage.mountPath` diverges from `engine-matrix-contract.md`'s column, cite T012's per-module finding in `OPEN-DECISIONS.md` rather than resolving it here.
- [ ] T012 Compare `specs/015-top-steam-game-modules/contracts/engine-matrix-contract.md`'s Storage Mount Path column against each shipped `modules/<name>/template.yaml`'s `spec.storage.mountPath` and write one per-module finding into `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` for every divergence (at minimum `palworld`, `rust`, `project-zomboid`, `dayz`, `garrys-mod`, `terraria`, `ark-survival-ascended`, `factorio`, `dont-starve-together`, `valheim`, `satisfactory`), each finding stating the shipped value, the contract value, and a recommendation; the shipped value stands until the maintainer rules on that finding. Depends on T005.
- [ ] T013 [P] Add a directory-layout rule to `modules/validate.py`, at **ERROR** severity (not WARN — confirmed by the maintainer), that fires when a module directory is missing `README.md`, `specs.md`, or a non-empty `samples/` directory — closing the gap where FR-002/SC-001/SC-003 are currently unenforced by the validator — and add a matching pass/fail-style self-check harness at `modules/test-validate-py.sh` (new file, following `modules/test-build-sh.sh`'s harness style — not added to `test-build-sh.sh` itself, whose own header scopes it to build.sh's signing preconditions) exercising the new rule against a fixture module missing each file in turn, and register it in the `gameplane-module` repo's own CI under `modules/.github/`. Scope the new rule with an explicit allowlist/opt-in marker (e.g. a per-module marker file, or an explicit list of the 26 in-scope module slugs) so the four modules outside this feature — `enshrouded`, `minecraft-java`, `nuclear-option`, `v-rising` — do not fail the new ERROR rule until they are separately brought up to layout.
- [ ] T014 [P] Add a shared `specs.md` section skeleton at `specs/015-top-steam-game-modules/contracts/specs-skeleton.md`, reproducing the 9 numbered headings from `specs/015-top-steam-game-modules/contracts/module-spec-contract.md` §2 verbatim (`# Gameplane Module Specification: <Display Name>` through `## 9. References & Upstream Documentation`), for the 26 per-module `specs.md` files authored in Phase 3 to be written against.
- [ ] T015 Edit `test/e2e/gamebot_helpers_e2e_test.go`, adding the 13 new game slugs to `fastGameSet` / `heavyGameSet` per the classification in `specs/015-top-steam-game-modules/engine-matrix-resolved.md` (T011) — `skipUnlessGameInScope` (gamebot_helpers_e2e_test.go:73) skips any game absent from both sets via `parseGameScope`, so every one of the 19 new e2e test tasks in Phases 4-7 depends on this task, not the reverse. Do not create a second helpers file — a duplicate copy would not change `skipUnlessGameInScope`'s behavior and risks package-level redeclaration. Bucket-registration in `test/e2e/buckets.sh` is a separate concern, handled by the dedicated tasks T075, T082, T095, T112, which are never `[P]` and always depend on the test tasks they register.

### 2.2 New rcon.protocol values: `rest` and `cli`

The maintainer has ruled that `rest` and `cli` become first-class `spec.rcon.protocol` enum values (additive, not breaking) so that HTTP-admin games (FiveM's txAdmin, Farming Simulator 25's web admin) and stdin-console games have a real protocol to declare instead of being faked with `palworld`/`satisfactory` or `none`+`consoleMode: pty`. `cli`'s exact behavior relative to the existing stdin/pod-attach path is an open question (T005) and must be answered before T019 is written; `rest` has no such ambiguity — it is a generic HTTP/JSON console client in the shape of `agent/internal/rcon/satisfactory.go`/`palworld.go`.

- [ ] T016 [P] Edit `operator/api/v1alpha1/gametemplate_types.go`: add `rest` and `cli` to the `+kubebuilder:validation:Enum=...` marker on `RCONSpec.Protocol` (~line 992), and add one doc-comment paragraph for each above it (~lines 975-995), in the same style as the existing per-protocol prose — `rest` is a generic HTTP/JSON console API (POST-per-command, bearer- or basic-auth, distinct from the bespoke `satisfactory`/`palworld` clients); `cli` is console access over the container's stdin/PTY, with its exact relationship to `consoleMode: pty` and `capabilities.actions[].transport` left to `OPEN-DECISIONS.md` (T005). In the SAME commit, run `make generate && make manifests` (CLAUDE.md rule 7) and commit the regenerated `operator/api/v1alpha1/zz_generated.deepcopy.go`, `operator/config/crd/*.yaml`, `operator/config/rbac/*.yaml`, `charts/gameplane/crds/*.yaml`, and `charts/gameplane/crd-manifests/*.yaml`.
- [ ] T017 Run `make module-schema` (`hack/gen-module-schema.py`) to regenerate `modules/.schema/gametemplate.schema.json` from the freshly-regenerated CRD; commit it in the `gameplane-module` repo and bump the submodule pointer here. Verify the regenerated `rcon.protocol` enum now contains exactly `source`, `telnet`, `websocket`, `battleye`, `satisfactory`, `palworld`, `nuclearoption`, `rest`, `cli`, `none` (10 values) — this run also closes the pre-existing drift where the generated schema was missing `nuclearoption` (already present in the Go CRD since before this feature), as a side effect of regeneration, not a hand-edit. Depends on T016.
- [ ] T018 [P] Author `agent/internal/rcon/rest.go` + `agent/internal/rcon/rest_test.go`: a generic HTTP/JSON console client modeled on `agent/internal/rcon/satisfactory.go`'s shape (lazy auth, a bounded response-size read, dial/request timeouts and an auth-failure cooldown as struct fields, loopback-scoped TLS handling if HTTPS is offered) but protocol-agnostic rather than Satisfactory's specific function-call envelope — document the wire contract this client actually speaks in the file's doc comment. Real unit tests required (agent is gated at 90% coverage).
- [ ] T019 Author `agent/internal/rcon/cli.go` + `agent/internal/rcon/cli_test.go`: console access over the container's stdin/PTY. Before writing any code, resolve the open question in `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` (T005) — does `cli` replace the existing pod-attach path, or does the agent itself drive stdin? — and implement exactly what that resolution says; document the decision in the file's doc comment. Real unit tests required (agent is gated at 90% coverage). Depends on T005.
- [ ] T020 Edit `agent/cmd/main.go`: add a `rest` and a `cli` case to both the `-rcon-protocol` flag's help text (~lines 68-73) and the dispatch `switch` that constructs `rconClient` (~lines 126-147), calling the new `rcon.NewREST`/`rcon.NewCLI` constructors from T018/T019. Depends on T018, T019.
- [ ] T021 Update `agent/specs.md`: add `rest` and `cli` (and the already-missing `nuclearoption`) to the `rcon/` line at ~line 49 and to the `--rcon-protocol` row at ~line 90, describing each client's wire contract as implemented in T018, T019 (Constitution Principle IV: `specs.md` must be updated in the same change that alters the behavior it documents). Depends on T018, T019, T020.
- [ ] T022 Add `rest` and `cli` to the `RCON_PROTOCOLS` tuple in `modules/validate.py` (~line 129), keeping it in sync with the CRD enum and `agent/internal/rcon/` per the tuple's own comment. Depends on T013 (both edit `modules/validate.py`; sequenced to avoid conflicting with the directory-layout rule change).
- [ ] T023 Update `docs/module-authoring.md`'s RCON section — change "Eight protocols are supported" to "Ten" and add `rest` and `cli` to the list at ~line 713, then add one worked `protocol:` subsection for each in the style of the existing `nuclearoption` block (~lines 806-816), pointing `cli` at the resolution recorded in `OPEN-DECISIONS.md` (T005). Depends on T016, T005.
- [ ] T024 [P] Audit `web/src/types.ts` and the rest of `web/src/` for any behavioral branch keyed on `rcon.protocol`, explicitly including `web/src/lib/capabilities.ts:20-23`'s `rconAvailable` (`proto !== "none"`) — not just a literal union or a hardcoded option list. `GameTemplate.spec.rcon.protocol` (types.ts:309) is already typed as a plain `string`, not a literal union, and a repo-wide search finds no dashboard component listing protocol values as options — record that finding, but do not skip `capabilities.ts:20-23`: once `rest`/`cli` are valid enum values, `rconAvailable` will advertise live-RCON dashboard actions for those protocols too; record whether that is correct or needs gating, and hand any required fix to T025.
- [ ] T025 Apply, in `operator/internal/controller/helpers.go` (`templateHasRCON`, ~line 94) and `operator/internal/controller/gameserver_rcon.go` (`resolveRCON`, ~line 41), whatever behavior the `cli` resolution recorded in `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` (T005) requires — e.g. whether a `<gs>-rcon` Secret is still minted for a passwordless stdin console — with envtest coverage for the change; if the resolution requires no operator change, record that explicitly in `OPEN-DECISIONS.md` rather than silently doing nothing. Depends on T005.

### 2.3 Gameplane-owned images for the auxiliary-service games

The maintainer has ruled that `fivem`, `farming-simulator-25`, `euro-truck-simulator-2`, and `beammp` get purpose-built Gameplane images instead of relying on an off-the-shelf community image, reversing `research.md` Decision 1, so that FR-012 (in-pod auxiliary services) and FR-013 (idle-with-diagnostics on a missing token) are guaranteed rather than hoped-for.

- [ ] T026 [P] Amend `specs/015-top-steam-game-modules/research.md` Decision 1: record the reversal — Gameplane now builds and signs purpose-built images for `fivem`, `farming-simulator-25`, `euro-truck-simulator-2`, and `beammp` instead of sourcing them from the community-image ecosystem, because only a Gameplane-owned entrypoint can guarantee FR-012's auxiliary-process supervision and FR-013's non-crashing diagnostic idle on every one of these four games; the other 22 modules are unaffected and keep community images. Rewrite the `**Feature**:` header to `015-top-steam-game-modules`.
- [ ] T027 [P] Author the FiveM Gameplane image: `modules/fivem/Dockerfile` (fxserver on top of a slim base) and its entrypoint (`modules/fivem/entrypoint.sh` or equivalent) supervising the CFX/txAdmin process and the embedded database (FR-012), and idling cleanly with a step-by-step diagnostic message when the CFX license key is unset (FR-013) instead of crash-looping.
- [ ] T028 [P] Author the Farming Simulator 25 Gameplane image: `modules/farming-simulator-25/Dockerfile` (headless Wine/Proton layer over the GIANTS dedicated server) and its entrypoint, supervising the web management portal (FR-012) alongside the game process, with X11/dummy-display setup isolated per spec.md's Edge Cases.
- [ ] T029 [P] Author the Euro Truck Simulator 2 Gameplane image: `modules/euro-truck-simulator-2/Dockerfile` and its entrypoint, idling cleanly with a step-by-step diagnostic message when the ETS2 server logon token is unset (FR-013) instead of crash-looping.
- [ ] T030 [P] Author the BeamMP Gameplane image: `modules/beammp/Dockerfile` and its entrypoint, idling cleanly with a step-by-step diagnostic message when the BeamMP auth key is unset (FR-013) instead of crash-looping.
- [ ] T031 Extend `modules/build.sh` (or add a sibling script, e.g. `modules/build-images.sh`, if `build.sh`'s existing per-module-directory loop can't cleanly accommodate a Dockerfile build step) to build and push the four images from T027-T030, and cosign-sign them consistent with how this repo signs published images (`cosign.pub` at the repo root is the verification key) — register the signing step in the `gameplane-module` repo's own CI under `modules/.github/`. The four templates (T047, T052, T053, T059) then pin the resulting images by `@sha256:` digest exactly like any community image, via T119's digest-pinning pass. Depends on T027, T028, T029, T030.

### 2.4 New-game wire-protocol probe packages

- [ ] T032 [P] Author the FiveM e2e probe package: `test/e2e/internal/fivem/app.go` (package main, standard `-addr`/`-deadline`/`-expect-depth`/`-expect-fail` flags, own retry loop) and `test/e2e/internal/fivem/spec.md`, reusing `test/e2e/internal/protocol/{a2sproto,sourceproto,joindepth}` where the wire format matches; prove the probe fails against a dead address and succeeds against a real listener before any task depends on it. Model on `test/e2e/internal/cs2/app.go`.
- [ ] T033 [P] Author the Team Fortress 2 e2e probe package: `test/e2e/internal/team-fortress-2/app.go` and `test/e2e/internal/team-fortress-2/spec.md` (Source/A2S_INFO family via `test/e2e/internal/protocol/{a2sproto,sourceproto}`); prove fail-dead/succeed-live per Constitution Principle I.
- [ ] T034 [P] Author the Farming Simulator 25 e2e probe package: `test/e2e/internal/farming-simulator-25/app.go` and `test/e2e/internal/farming-simulator-25/spec.md` (GIANTS/HTTP query); prove fail-dead/succeed-live.
- [ ] T035 [P] Author the Euro Truck Simulator 2 e2e probe package: `test/e2e/internal/euro-truck-simulator-2/app.go` and `test/e2e/internal/euro-truck-simulator-2/spec.md` (A2S_INFO via `test/e2e/internal/protocol/a2sproto`); prove fail-dead/succeed-live.
- [ ] T036 [P] Author the Mount & Blade II: Bannerlord e2e probe package: `test/e2e/internal/mount-and-blade-2-bannerlord/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`); prove fail-dead/succeed-live.
- [ ] T037 [P] Author the tModLoader e2e probe package: `test/e2e/internal/tmodloader/app.go` and `.../spec.md` (Terraria-family custom TCP via `test/e2e/internal/protocol/joindepth`); prove fail-dead/succeed-live.
- [ ] T038 [P] Author the BeamMP e2e probe package: `test/e2e/internal/beammp/app.go` and `.../spec.md` (custom TCP/UDP handshake); prove fail-dead/succeed-live.
- [ ] T039 [P] Author the Left 4 Dead 2 e2e probe package: `test/e2e/internal/left-4-dead-2/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`); prove fail-dead/succeed-live.
- [ ] T040 [P] Author The Isle e2e probe package: `test/e2e/internal/the-isle/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`); prove fail-dead/succeed-live.
- [ ] T041 [P] Author the ARK: Survival Evolved e2e probe package: `test/e2e/internal/ark-survival-evolved/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`, cluster-aware per `joindepth` where applicable); prove fail-dead/succeed-live.
- [ ] T042 [P] Author the Arma Reforger e2e probe package: `test/e2e/internal/arma-reforger/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`); prove fail-dead/succeed-live.
- [ ] T043 [P] Author the Hell Let Loose e2e probe package: `test/e2e/internal/hell-let-loose/app.go` and `.../spec.md` (A2S_INFO via `a2sproto`); prove fail-dead/succeed-live.
- [ ] T044 [P] Author the Squad e2e probe package: `test/e2e/internal/squad/app.go` and `.../spec.md` (Source-family A2S_INFO via `a2sproto`/`sourceproto`); prove fail-dead/succeed-live.

**Checkpoint**: `OPEN-DECISIONS.md` and `engine-matrix-resolved.md` exist; the contract/data-model artifacts match the shipped CRD; `rest` and `cli` are real, agent-implemented `rcon.protocol` values; the four Gameplane-owned images exist and are signed; the validator enforces layout and knows about `rest`/`cli`; the specs skeleton is in place; `gamebot_helpers_e2e_test.go` recognizes all 13 new games; every new game has a proven wire-protocol probe package. User story work can now begin.

---

## Phase 3: User Story 1 - One-Click Deployment for Top Steam Multiplayer Dedicated Servers (Priority: P1) 🎯 MVP

**Goal**: Every one of the 26 top-Steam-multiplayer games has a complete, schema-valid module package that boots into a healthy, ready-to-accept-players state with correct ports and storage.

**Independent Test**: Instantiate any of the 26 modules with default parameters and verify the server boots to healthy with correct port exposure and storage persistence (spec.md US1 Independent Test).

**Scope note**: this phase authors each module's `template.yaml` **once, completely** — storage, rcon, lifecycle, mods, and probes stanzas all included per `specs/015-top-steam-game-modules/engine-matrix-resolved.md` and the house style of `modules/rust/template.yaml` — because a module is not deployable without all of them together. User Stories 2-5 do not re-author these files; they verify, extend narrowly, and write tests against what is authored here. Treat every "audit" task in Phases 4-7 as reading and, where needed, incrementally adjusting these files — not rewriting them. All 13 existing modules already ship `module.yaml`, `template.yaml` and `README.md` — those are **verified and updated**, never authored fresh; only `specs.md` (all 26) and `samples/` (7 of the 13 existing modules) are genuinely missing. T071/T072 close the FR-006 security-context gap for all 26 modules once every `template.yaml` in this phase is settled.

Of the 13 target *existing* modules, 7 have no `samples/` directory today and get one authored here for the first time: `project-zomboid`, `dayz`, `garrys-mod`, `7-days-to-die`, `ark-survival-ascended`, `dont-starve-together`, `satisfactory`. The other 6 existing modules (`cs2`, `palworld`, `rust`, `terraria`, `factorio`, `valheim`) already have `samples/gameserver.yaml` and get it verified/updated instead. **None** of the 26 modules has a `specs.md` today — every task below authors one.

- [ ] T045 [P] [US1] Standardize Counter-Strike 2 (`cs2`, existing module): verify/update the existing `modules/cs2/module.yaml` against `modules/.schema/module.schema.json`, and author `modules/cs2/specs.md` (per `specs/015-top-steam-game-modules/contracts/specs-skeleton.md`); bring `modules/cs2/template.yaml`'s storage/rcon(`source`)/lifecycle/probes stanzas in line with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/cs2/README.md`; verify `modules/cs2/samples/gameserver.yaml` against the standardized template.
- [ ] T046 [P] [US1] Standardize Palworld (`palworld`, existing module): verify/update the existing `modules/palworld/module.yaml` and author `modules/palworld/specs.md`; align `modules/palworld/template.yaml`'s storage/rcon(`palworld`)/lifecycle/probes stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/palworld/README.md`; verify `modules/palworld/samples/gameserver.yaml`.
- [ ] T047 [P] [US1] Create FiveM (`fivem`, new module): author `modules/fivem/module.yaml`, `modules/fivem/template.yaml` (full storage/rcon(`rest`, targeting txAdmin's HTTP admin API via the agent's new generic REST client)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`, house style from `modules/rust/template.yaml`, plus the FR-012 single-pod txAdmin/database bundling and the FR-013 graceful-idle diagnostic entrypoint for a missing CFX license key, both provided by the Gameplane-owned image from T027), `modules/fivem/README.md`, `modules/fivem/specs.md`, `modules/fivem/samples/gameserver.yaml`. Depends on T016, T020, T027 (needs the CRD enum, the agent dispatch switch, and the built image).
- [ ] T048 [P] [US1] Standardize Rust (`rust`, existing module, house-style reference): verify/update the existing `modules/rust/module.yaml` and author `modules/rust/specs.md`; `modules/rust/template.yaml` is already the feature's style exemplar — **verify only** against `specs/015-top-steam-game-modules/engine-matrix-resolved.md`, do not rewrite it; any change to `modules/rust/template.yaml` must be raised as a separate, non-`[P]` task since 24 other Phase 3 tasks cite it as their house-style reference; update `modules/rust/README.md`; verify `modules/rust/samples/gameserver.yaml`.
- [ ] T049 [P] [US1] Standardize Project Zomboid (`project-zomboid`, existing module): verify/update the existing `modules/project-zomboid/module.yaml` and author `modules/project-zomboid/specs.md`; align `modules/project-zomboid/template.yaml`'s storage/rcon/lifecycle/probes stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md` — the module ships `rcon.protocol: source` today; whether this or the contract's stdin-only claim is correct is under investigation in `OPEN-DECISIONS.md` (T005), so this task verifies the template **as shipped**, it does not change the protocol; update `modules/project-zomboid/README.md`; author `modules/project-zomboid/samples/gameserver.yaml` (currently missing).
- [ ] T050 [P] [US1] Create Team Fortress 2 (`team-fortress-2`, new module): author `modules/team-fortress-2/module.yaml`, `modules/team-fortress-2/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`, house style from `modules/rust/template.yaml`), `modules/team-fortress-2/README.md`, `modules/team-fortress-2/specs.md`, `modules/team-fortress-2/samples/gameserver.yaml`.
- [ ] T051 [P] [US1] Standardize DayZ (`dayz`, existing module): verify/update the existing `modules/dayz/module.yaml` and author `modules/dayz/specs.md`; align `modules/dayz/template.yaml`'s storage/rcon(`battleye`)/lifecycle/probes stanzas and Steam Workshop mod paths with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/dayz/README.md`; author `modules/dayz/samples/gameserver.yaml` (currently missing).
- [ ] T052 [P] [US1] Create Farming Simulator 25 (`farming-simulator-25`, new module): author `modules/farming-simulator-25/module.yaml`, `modules/farming-simulator-25/template.yaml` (using the Gameplane-owned image from T028, `rcon: rest` targeting the web management portal's HTTP API via the agent's new generic REST client, the web admin port declared under `spec.ports`, single-pod bundling of the web management portal per FR-012), `modules/farming-simulator-25/README.md`, `modules/farming-simulator-25/specs.md`, `modules/farming-simulator-25/samples/gameserver.yaml`. Depends on T016, T020, T028.
- [ ] T053 [P] [US1] Create Euro Truck Simulator 2 (`euro-truck-simulator-2`, new module): author `modules/euro-truck-simulator-2/module.yaml`, `modules/euro-truck-simulator-2/template.yaml` (`rcon: none` + `consoleMode: pty`, ETS2 logon token via `spec.env` with the FR-013 graceful-idle diagnostic when it's missing, using the Gameplane-owned image from T029), `modules/euro-truck-simulator-2/README.md`, `modules/euro-truck-simulator-2/specs.md`, `modules/euro-truck-simulator-2/samples/gameserver.yaml`. Depends on T029.
- [ ] T054 [P] [US1] Standardize Garry's Mod (`garrys-mod`, existing module): verify/update the existing `modules/garrys-mod/module.yaml` and author `modules/garrys-mod/specs.md`; confirm `modules/garrys-mod/template.yaml`'s `rcon.protocol: none` with no `capabilities` block against `specs/015-top-steam-game-modules/engine-matrix-resolved.md` — this is a deliberate, documented omission (no RCON reachable; see the template's header comment), now also confirmed authoritative by T009's corrected `engine-matrix-contract.md` row — not a gap to fill on this task; align only the storage/probes stanzas; update `modules/garrys-mod/README.md`; author `modules/garrys-mod/samples/gameserver.yaml` (currently missing). Steam Workshop collection support is out of scope here — see T096.
- [ ] T055 [P] [US1] Create Mount & Blade II: Bannerlord (`mount-and-blade-2-bannerlord`, new module): author `modules/mount-and-blade-2-bannerlord/module.yaml`, `modules/mount-and-blade-2-bannerlord/template.yaml` (`rcon: none` + `consoleMode: pty`, match-based server with no persistent world save), `modules/mount-and-blade-2-bannerlord/README.md`, `modules/mount-and-blade-2-bannerlord/specs.md`, `modules/mount-and-blade-2-bannerlord/samples/gameserver.yaml`.
- [ ] T056 [P] [US1] Standardize Terraria (`terraria`, existing module): verify/update the existing `modules/terraria/module.yaml` and author `modules/terraria/specs.md`; align `modules/terraria/template.yaml`'s storage/rcon(`none`)+`consoleMode: pty`/lifecycle/probes stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/terraria/README.md`; verify `modules/terraria/samples/gameserver.yaml`.
- [ ] T057 [P] [US1] Standardize 7 Days to Die (`7-days-to-die`, existing module): verify/update the existing `modules/7-days-to-die/module.yaml` and author `modules/7-days-to-die/specs.md`; confirm `modules/7-days-to-die/template.yaml`'s `rcon.protocol: none` with no `capabilities` block against `specs/015-top-steam-game-modules/engine-matrix-resolved.md` — settled by T009's corrected `engine-matrix-contract.md` row (TelnetPassword lives in serverconfig.xml under serverfiles/, unreachable from the world-saves mount); note the module's declared telnet port is 8081, not the (now-corrected) engine-matrix-contract's former 8082 — hand that divergence to T089, which records it in `OPEN-DECISIONS.md`; do not silently renumber it; align only the storage/probes stanzas; update `modules/7-days-to-die/README.md`; author `modules/7-days-to-die/samples/gameserver.yaml` (currently missing).
- [ ] T058 [P] [US1] Create tModLoader (`tmodloader`, new module): author `modules/tmodloader/module.yaml`, `modules/tmodloader/template.yaml` (`rcon: none` + `consoleMode: pty`, Terraria modding framework layout, `capabilities.mods` workshop/mod-install path per FR-010), `modules/tmodloader/README.md`, `modules/tmodloader/specs.md`, `modules/tmodloader/samples/gameserver.yaml`.
- [ ] T059 [P] [US1] Create BeamMP (`beammp`, new module): author `modules/beammp/module.yaml`, `modules/beammp/template.yaml` (`rcon: none` + `consoleMode: pty`, BeamMP auth-key FR-013 graceful-idle diagnostic using the Gameplane-owned image from T030, custom vehicles/maps mount under `capabilities.mods`), `modules/beammp/README.md`, `modules/beammp/specs.md`, `modules/beammp/samples/gameserver.yaml`. Depends on T030.
- [ ] T060 [P] [US1] Standardize ARK: Survival Ascended (`ark-survival-ascended`, existing module): verify/update the existing `modules/ark-survival-ascended/module.yaml` and author `modules/ark-survival-ascended/specs.md`; align `modules/ark-survival-ascended/template.yaml`'s storage/rcon(`source`)/lifecycle/probes stanzas and cluster-travel storage layout with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/ark-survival-ascended/README.md`; author `modules/ark-survival-ascended/samples/gameserver.yaml` (currently missing).
- [ ] T061 [P] [US1] Create Left 4 Dead 2 (`left-4-dead-2`, new module): author `modules/left-4-dead-2/module.yaml`, `modules/left-4-dead-2/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`), `modules/left-4-dead-2/README.md`, `modules/left-4-dead-2/specs.md`, `modules/left-4-dead-2/samples/gameserver.yaml`.
- [ ] T062 [P] [US1] Standardize Factorio (`factorio`, existing module): verify/update the existing `modules/factorio/module.yaml` and author `modules/factorio/specs.md`; align `modules/factorio/template.yaml`'s storage/lifecycle/probes stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md` — the module ships `rcon.protocol: source` **with** `consoleMode: pty` today; whether this or the contract's `none`+pty claim is correct is under investigation in `OPEN-DECISIONS.md` (T005), so this task verifies the template **as shipped**, it does not change the protocol; update `modules/factorio/README.md`; verify `modules/factorio/samples/gameserver.yaml`.
- [ ] T063 [P] [US1] Create The Isle (`the-isle`, new module): author `modules/the-isle/module.yaml`, `modules/the-isle/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`), `modules/the-isle/README.md`, `modules/the-isle/specs.md`, `modules/the-isle/samples/gameserver.yaml`.
- [ ] T064 [P] [US1] Standardize Don't Starve Together (`dont-starve-together`, existing module): verify/update the existing `modules/dont-starve-together/module.yaml` and author `modules/dont-starve-together/specs.md`; align `modules/dont-starve-together/template.yaml`'s storage/rcon(`none`)+`consoleMode: pty` Lua console/lifecycle/probes stanzas and master/caves multi-shard storage layout with `specs/015-top-steam-game-modules/engine-matrix-resolved.md` — note this module ships a `capabilities.lifecycle.stop` array under `rcon.protocol: none` (the unresolved `OPEN-DECISIONS.md` ambiguity from T005); do not remove it; update `modules/dont-starve-together/README.md`; author `modules/dont-starve-together/samples/gameserver.yaml` (currently missing).
- [ ] T065 [P] [US1] Standardize Valheim (`valheim`, existing module): verify/update the existing `modules/valheim/module.yaml` and author `modules/valheim/specs.md`; align `modules/valheim/template.yaml`'s storage/rcon/lifecycle/probes and BepInEx mod-loader stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/valheim/README.md`; verify `modules/valheim/samples/gameserver.yaml`.
- [ ] T066 [P] [US1] Standardize Satisfactory (`satisfactory`, existing module): verify/update the existing `modules/satisfactory/module.yaml` and author `modules/satisfactory/specs.md`; align `modules/satisfactory/template.yaml`'s storage/rcon(`satisfactory`)/lifecycle/probes stanzas with `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; update `modules/satisfactory/README.md`; author `modules/satisfactory/samples/gameserver.yaml` (currently missing).
- [ ] T067 [US1] Create ARK: Survival Evolved (`ark-survival-evolved`, new module): author `modules/ark-survival-evolved/module.yaml`, `modules/ark-survival-evolved/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`, cluster-travel storage layout consistent with `ark-survival-ascended`), `modules/ark-survival-evolved/README.md`, `modules/ark-survival-evolved/specs.md`, `modules/ark-survival-evolved/samples/gameserver.yaml`. Not `[P]`: requires `modules/ark-survival-ascended/template.yaml`'s finished cluster-travel layout. Depends on T060.
- [ ] T068 [P] [US1] Create Arma Reforger (`arma-reforger`, new module): author `modules/arma-reforger/module.yaml`, `modules/arma-reforger/template.yaml` (`rcon: none` + `consoleMode: pty`, authenticated-SteamCMD-login note per spec.md Edge Cases, Steam Workshop mod support), `modules/arma-reforger/README.md`, `modules/arma-reforger/specs.md`, `modules/arma-reforger/samples/gameserver.yaml`.
- [ ] T069 [P] [US1] Create Hell Let Loose (`hell-let-loose`, new module): author `modules/hell-let-loose/module.yaml`, `modules/hell-let-loose/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`; `spec.versions` includes the default Hell Let Loose build plus an optional Vietnam community-preset version, per spec.md Clarifications — see also T119's digest-pinning pass), `modules/hell-let-loose/README.md`, `modules/hell-let-loose/specs.md`, `modules/hell-let-loose/samples/gameserver.yaml`.
- [ ] T070 [P] [US1] Create Squad (`squad`, new module): author `modules/squad/module.yaml`, `modules/squad/template.yaml` (storage/rcon(`source`)/lifecycle/probes per `specs/015-top-steam-game-modules/engine-matrix-resolved.md`), `modules/squad/README.md`, `modules/squad/specs.md`, `modules/squad/samples/gameserver.yaml`.
- [ ] T071 [US1] Audit `spec.security.runAsUser`/`runAsGroup`/`fsGroup` and the `HOME` entry in `spec.env` in the 13 **new** modules' `template.yaml` against `modules/validate.py` rule 2 (image `User` must match `runAsUser`), rule 3 (`runAsUser` + SteamCMD-shaped image requires `HOME`) and rule 8 (PUID/PGID images need `fsGroup`), satisfying FR-006. Modules: `fivem`, `team-fortress-2`, `farming-simulator-25`, `euro-truck-simulator-2`, `mount-and-blade-2-bannerlord`, `tmodloader`, `beammp`, `left-4-dead-2`, `the-isle`, `ark-survival-evolved`, `arma-reforger`, `hell-let-loose`, `squad`. Depends on T047, T050, T052, T053, T055, T058, T059, T061, T063, T067, T068, T069, T070.
- [ ] T072 [US1] Audit `spec.security.runAsUser`/`runAsGroup`/`fsGroup` and the `HOME` entry in `spec.env` in the 13 **existing** modules' `template.yaml` against the same `modules/validate.py` rules 2/3/8, satisfying FR-006. Modules: `cs2`, `palworld`, `rust`, `project-zomboid`, `dayz`, `garrys-mod`, `terraria`, `7-days-to-die`, `ark-survival-ascended`, `factorio`, `dont-starve-together`, `valheim`, `satisfactory`. Depends on T045, T046, T048, T049, T051, T054, T056, T057, T060, T062, T064, T065, T066.

**Checkpoint**: all 26 modules have a complete, schema-conformant package, including verified security-context wiring. User Story 1 is independently deployable and testable.

---

## Phase 4: User Story 2 - Persistent Game State, World Saves, and Multi-Shard Clustering (Priority: P1)

**Goal**: Savegames, world files, and configs survive container restarts and image upgrades without corruption; multi-instance games (DST master/caves, ARK cluster travel) keep cross-shard state consistent. SC-005's "100% of game servers restart without data loss" is verified here by **sampling**: T073/T074 cover one representative of the two storage topologies this feature introduces (a single-volume game and a cluster-travel multi-shard game); the remaining 24 modules are covered by the storage/mountPath audits T076-T079, not by a dedicated restart test each — this is a sampling choice, not full coverage, and is recorded as such rather than claimed as 100% exercised.

**Independent Test**: Start a server, generate in-game state, restart the container, and verify all world data/configs/saves are preserved without corruption (spec.md US2 Independent Test).

- [ ] T073 [P] [US2] Author `test/e2e/fivem_persistence_e2e_test.go`: write a marker via the FiveM data volume, restart the `fivem` GameServer, and assert the marker and world/config state under `modules/fivem`'s `storage.mountPath` survive the restart. Depends on T032 (fivem probe package proven).
- [ ] T074 [P] [US2] Author `test/e2e/arksurvival_cluster_persistence_e2e_test.go`: verify ARK: Survival Ascended and ARK: Survival Evolved cluster-travel shard storage stays consistent across a restart of either shard. Depends on T041 (ark-survival-evolved probe package proven).
- [ ] T075 [US2] Register the two new test names from T073/T074 into `test/e2e/buckets.sh`; assign each test to the bucket `specs/015-top-steam-game-modules/engine-matrix-resolved.md`'s (T011) classification column derives, using the same disk/memory criteria as `bucket_bot_heavy`'s existing per-test comment block (any SteamCMD multi-GB download or >5Gi storage goes heavy), with a per-test CI-exclusion comment matching the existing entries' style for any heavy assignment. Depends on T073, T074.
- [ ] T076 [US2] Audit `storage.mountPath` safety and sizing against `specs/015-top-steam-game-modules/engine-matrix-resolved.md` and `modules/validate.py` Rule 4 (no shadowing) for the 9 lightweight new modules: `modules/fivem/template.yaml`, `modules/team-fortress-2/template.yaml`, `modules/farming-simulator-25/template.yaml`, `modules/euro-truck-simulator-2/template.yaml`, `modules/mount-and-blade-2-bannerlord/template.yaml`, `modules/tmodloader/template.yaml`, `modules/beammp/template.yaml`, `modules/left-4-dead-2/template.yaml`, `modules/the-isle/template.yaml`. Where `engine-matrix-resolved.md`/T011 records a mount-path divergence finding (T012) for any of these modules, cite that finding rather than treating the shipped value as agreed.
- [ ] T077 [US2] Audit `storage.mountPath` safety, sizing, and cluster/multi-shard wiring for the three remaining heavy new modules: `modules/squad/template.yaml`, `modules/arma-reforger/template.yaml`, `modules/hell-let-loose/template.yaml`. Where `engine-matrix-resolved.md`/T011 records a mount-path divergence finding (T012) for any of these modules, cite that finding rather than treating the shipped value as agreed.
- [ ] T078 [US2] Audit ARK cluster-travel storage topology consistency between `modules/ark-survival-ascended/template.yaml` (existing) and `modules/ark-survival-evolved/template.yaml` (new) so shared cluster identifiers and volume layouts match. Where `engine-matrix-resolved.md`/T011 records a mount-path divergence finding (T012) for either module, cite that finding rather than treating the shipped value as agreed.
- [ ] T079 [US2] Audit the Don't Starve Together master/caves multi-shard storage layout in `modules/dont-starve-together/template.yaml` and `modules/dont-starve-together/README.md`. Where `engine-matrix-resolved.md`/T011 records a mount-path divergence finding (T012) for this module, cite that finding rather than treating the shipped value as agreed.

**Checkpoint**: persistence is verified for every heavy/clustered module and exercised by a real restart e2e test; User Stories 1 and 2 both work independently.

---

## Phase 5: User Story 3 - Interactive Administration, Remote Console (RCON/API), and Operational Actions (Priority: P2)

**Goal**: Moderators can issue live admin commands and trigger a graceful, save-then-stop shutdown appropriate to each engine. SC-006's "100% of modules with remote console interfaces successfully execute administrative commands and graceful save-on-shutdown" is verified here by **sampling** one Source-RCON-family module (`team-fortress-2`) and one Source-family cluster module (`squad`) with dedicated e2e tests (T080/T081); the remaining RCON-capable modules are covered by the protocol-family audits T083-T092, not each by its own RCON e2e test. `capabilities.lifecycle.stop` is documented-but-not-CEL-enforced to require `rcon.protocol != none` (see `OPEN-DECISIONS.md`, T005) — `modules/dont-starve-together/template.yaml` ships a working `stop` sequence under `rcon.protocol: none`, so no audit task in this phase may delete a `none`-protocol module's `capabilities.lifecycle.stop` on the strength of the schema's prose alone.

**Independent Test**: Issue an administrative command via the supported control protocol to a running server and observe the response; trigger a stop action and observe the pre-shutdown save sequence (spec.md US3 Independent Test).

- [ ] T080 [P] [US3] Author `test/e2e/teamfortress2_rcon_e2e_test.go`: issue a Source RCON admin command against a running `team-fortress-2` server and verify the response, then trigger stop and verify `modules/team-fortress-2/template.yaml`'s `capabilities.lifecycle.stop` sequence runs before the pod terminates. Depends on T033 (team-fortress-2 probe package proven).
- [ ] T081 [P] [US3] Author `test/e2e/squad_rcon_e2e_test.go`: issue a Source-family RCON admin command against a running `squad` server and verify the response, then trigger stop and verify `modules/squad/template.yaml`'s `capabilities.lifecycle.stop` sequence. Depends on T044 (squad probe package proven).
- [ ] T082 [US3] Register the two new test names from T080/T081 into `test/e2e/buckets.sh`; assign each test to the bucket `specs/015-top-steam-game-modules/engine-matrix-resolved.md`'s (T011) classification column derives, using the same disk/memory criteria as `bucket_bot_heavy`'s existing per-test comment block (any SteamCMD multi-GB download or >5Gi storage goes heavy), with a CI-exclusion comment for any heavy assignment. Depends on T080, T081.
- [ ] T083 [US3] Audit `source`-protocol `rcon`/`capabilities.lifecycle.stop`/`capabilities.actions` stanzas against `specs/015-top-steam-game-modules/engine-matrix-resolved.md` for `modules/cs2/template.yaml`, `modules/team-fortress-2/template.yaml`, `modules/left-4-dead-2/template.yaml`, `modules/factorio/template.yaml` (source protocol **with** `consoleMode: pty` — confirm both stanzas coexist correctly; this pairing is still under maintainer investigation in `OPEN-DECISIONS.md` (T005), not yet settled), `modules/project-zomboid/template.yaml` (source protocol; likewise still under investigation per T005).
- [ ] T084 [US3] Audit `source`-protocol `rcon`/`capabilities.lifecycle.stop`/`capabilities.actions` stanzas for `modules/squad/template.yaml`, `modules/the-isle/template.yaml`, `modules/hell-let-loose/template.yaml`.
- [ ] T085 [US3] Audit `source`-protocol `rcon`/`capabilities.lifecycle.stop` cluster consistency (shared `SaveWorld` stop command) across `modules/ark-survival-ascended/template.yaml` and `modules/ark-survival-evolved/template.yaml`.
- [ ] T086 [US3] Audit the `battleye`-protocol `rcon`/`capabilities.lifecycle.stop` stanza in `modules/dayz/template.yaml`. Confirm the documented deliberate absence of a full `capabilities.lifecycle.stop` still holds (BattlEye RCon has a `#shutdown` command, but DayZ's save semantics around it are the caveat the template's header records), or record a change request in `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` for maintainer sign-off — do not add a stop stanza on the strength of the engine matrix alone.
- [ ] T087 [US3] Verify the `websocket`-protocol `rcon`/`capabilities.lifecycle.stop` stanza in `modules/rust/template.yaml` against `specs/015-top-steam-game-modules/engine-matrix-resolved.md` (this is the house-style reference — confirm no drift, do not restructure it).
- [ ] T088 [US3] Audit the `satisfactory`- and `palworld`-protocol `rcon`/`capabilities.lifecycle.stop` stanzas in `modules/satisfactory/template.yaml` and `modules/palworld/template.yaml`.
- [ ] T089 [US3] Audit the `rcon.protocol: none`, no-`capabilities`-block modules `modules/garrys-mod/template.yaml` and `modules/7-days-to-die/template.yaml`, confirmed authoritative by T009's corrected `engine-matrix-contract.md` rows: confirm the documented deliberate absence of RCON and `capabilities.lifecycle.stop` still holds — garrys-mod has no console reachable at all; 7-days-to-die's LinuxGSM entrypoint (user.sh) already traps SIGINT/SIGTERM and runs `sdtdserver stop` on its own, so the omission is not a gap — or record a change request in `OPEN-DECISIONS.md` for maintainer sign-off; do not add a stop stanza on the strength of the engine matrix alone. Also record, in `specs/015-top-steam-game-modules/OPEN-DECISIONS.md`, 7-days-to-die's declared telnet port (8081) vs. the engine-matrix-contract's port (per T057) as an open item, not something to silently renumber.
- [ ] T090 [US3] Audit `none` + `consoleMode: pty` stdin-console `capabilities.lifecycle.stop` stanzas for the existing modules `modules/terraria/template.yaml` and `modules/dont-starve-together/template.yaml` — both are genuinely `rcon.protocol: none` (unlike factorio/project-zomboid, whose protocol is still under investigation — see `OPEN-DECISIONS.md`, T005); dont-starve-together ships a working `stop` array despite `rcon.protocol: none` (the unresolved `OPEN-DECISIONS.md` ambiguity) — do not remove it here.
- [ ] T091 [US3] Audit `none` + `consoleMode: pty` stdin-console `capabilities.lifecycle.stop` stanzas for `modules/euro-truck-simulator-2/template.yaml`, `modules/mount-and-blade-2-bannerlord/template.yaml`, `modules/arma-reforger/template.yaml`.
- [ ] T092 [US3] Audit `capabilities.lifecycle.stop` stanzas and the FR-013 graceful-idle-on-missing-token diagnostics for `modules/fivem/template.yaml` (`rcon: rest`), `modules/beammp/template.yaml`, `modules/tmodloader/template.yaml`, `modules/farming-simulator-25/template.yaml` (`rcon: rest`). Depends on T027, T028, T030 (the Gameplane-owned images supplying the diagnostic-idle entrypoints for fivem/farming-simulator-25/beammp).

**Checkpoint**: every protocol family's admin/lifecycle stanza is verified and two real RCON e2e tests exist; User Stories 1-3 work independently.

---

## Phase 6: User Story 4 - Automated Modding, Frameworks, and Workshop Synchronization (Priority: P2)

**Goal**: Mods, workshop collections, and modding frameworks install and load through declared `capabilities.mods` paths, without hand-edited entrypoints.

**Independent Test**: Deploy a modded framework module with a workshop collection ID or mod list, boot it, and verify the mods are downloaded, mounted, and loaded (spec.md US4 Independent Test).

- [ ] T093 [P] [US4] Author `test/e2e/tmodloader_mods_e2e_test.go`: verify a configured mod is downloaded/mounted/loaded under `modules/tmodloader/template.yaml`'s `capabilities.mods` layout. Depends on T037 (tmodloader probe package proven).
- [ ] T094 [P] [US4] Author `test/e2e/garrysmod_workshop_e2e_test.go`: verify a Steam Workshop collection ID supplied to `modules/garrys-mod` mounts and activates during startup.
- [ ] T095 [US4] Register the two new test names from T093/T094 into `test/e2e/buckets.sh`; assign each test to the bucket `specs/015-top-steam-game-modules/engine-matrix-resolved.md`'s (T011) classification column derives, using the same disk/memory criteria as `bucket_bot_heavy`'s existing per-test comment block (any SteamCMD multi-GB download or >5Gi storage goes heavy). Depends on T093, T094.
- [ ] T096 [US4] Author `capabilities.mods` Steam Workshop wiring (FR-010) — currently absent — for `modules/dayz/template.yaml`, `modules/squad/template.yaml`, `modules/arma-reforger/template.yaml`. For `modules/garrys-mod/template.yaml`, `modules/7-days-to-die/template.yaml`, and `modules/project-zomboid/template.yaml`: none of the three ships a `capabilities.mods` block today, and garrys-mod's template header states the omission is deliberate ("No capabilities block either: with no console reachable at all…") — flag that adding mods to these three reverses a documented deliberate omission, and get maintainer sign-off in `specs/015-top-steam-game-modules/OPEN-DECISIONS.md` before making the edit.
- [ ] T097 [US4] Audit `capabilities.mods` dedicated resource-directory/plugin-path layouts for the framework modules `modules/tmodloader/template.yaml`, `modules/fivem/template.yaml`, `modules/beammp/template.yaml`.
- [ ] T098 [US4] Audit `capabilities.mods.path` (not `.loaders` — CS2 has no loader, its addons resolve via a fixed `game/csgo/addons` path) in `modules/cs2/template.yaml`, and `capabilities.mods.loaders` (bepinex → bepinex/plugins) in `modules/valheim/template.yaml`, confirming addon paths never shadow `storage.mountPath` per `modules/validate.py` Rule 4, and that valheim's non-empty `spec.versions` satisfies the CEL rule gating `loaders`.

**Checkpoint**: workshop and framework mod paths are verified with real e2e coverage; User Stories 1-4 work independently.

---

## Phase 7: User Story 5 - Automated Health Probing & Protocol Verification (Priority: P3)

**Goal**: Every server's status accurately reflects wire-level readiness (Starting/Ready/Unhealthy), never just "container running." Constitution Principle I's probe-trust rule (a probe must be proven to fail against a dead address and succeed against a real listener before it is trusted) is satisfied by the fail/succeed proof bundled into each Phase 2 probe-package task (T032-T044), not re-proven here.

**Independent Test**: Query a running server via its native protocol and observe correct status transitions between Initializing, Ready, Unhealthy, and Stopped (spec.md US5 Independent Test).

- [ ] T099 [P] [US5] Author `test/e2e/fivem_bot_e2e_test.go`: query the `fivem` server via its probe package and assert the Ready transition and returned player-count metrics. Depends on T032.
- [ ] T100 [P] [US5] Author `test/e2e/teamfortress2_bot_e2e_test.go`: A2S_INFO health probe against `team-fortress-2`. Depends on T033.
- [ ] T101 [P] [US5] Author `test/e2e/farmingsimulator25_bot_e2e_test.go`: GIANTS/HTTP health probe against `farming-simulator-25`. Depends on T034.
- [ ] T102 [P] [US5] Author `test/e2e/eurotrucksimulator2_bot_e2e_test.go`: A2S_INFO health probe against `euro-truck-simulator-2`. Depends on T035.
- [ ] T103 [P] [US5] Author `test/e2e/mountandblade2bannerlord_bot_e2e_test.go`: A2S_INFO health probe against `mount-and-blade-2-bannerlord`. Depends on T036.
- [ ] T104 [P] [US5] Author `test/e2e/tmodloader_bot_e2e_test.go`: custom-TCP (Terraria-family) health probe against `tmodloader`. Depends on T037.
- [ ] T105 [P] [US5] Author `test/e2e/beammp_bot_e2e_test.go`: custom TCP/UDP health probe against `beammp`. Depends on T038.
- [ ] T106 [P] [US5] Author `test/e2e/left4dead2_bot_e2e_test.go`: A2S_INFO health probe against `left-4-dead-2`. Depends on T039.
- [ ] T107 [P] [US5] Author `test/e2e/theisle_bot_e2e_test.go`: A2S_INFO health probe against `the-isle`. Depends on T040.
- [ ] T108 [P] [US5] Author `test/e2e/arksurvivalevolved_bot_e2e_test.go`: A2S_INFO health probe against `ark-survival-evolved`, heavy game. Depends on T041.
- [ ] T109 [P] [US5] Author `test/e2e/armareforger_bot_e2e_test.go`: A2S_INFO health probe against `arma-reforger`, heavy game. Depends on T042.
- [ ] T110 [P] [US5] Author `test/e2e/hellletloose_bot_e2e_test.go`: A2S_INFO health probe against `hell-let-loose`, heavy game. Depends on T043.
- [ ] T111 [P] [US5] Author `test/e2e/squad_bot_e2e_test.go`: A2S_INFO health probe against `squad`, heavy game (distinct file/test from the RCON coverage in T081). Depends on T044.
- [ ] T112 [US5] Register the 13 new test names from T099-T111 into `test/e2e/buckets.sh`; assign each test to the bucket `specs/015-top-steam-game-modules/engine-matrix-resolved.md`'s (T011) classification column derives, using the same disk/memory criteria as `bucket_bot_heavy`'s existing per-test comment block (any SteamCMD multi-GB download or >5Gi storage goes heavy), with a per-test CI-exclusion comment matching the existing entries' style for each heavy assignment — note `bucket_bot_heavy` is deliberately never executed by any CI job, matching the existing precedent. Depends on T099, T100, T101, T102, T103, T104, T105, T106, T107, T108, T109, T110, T111.
- [ ] T113 [US5] Audit `spec.probes.readiness` as a plain Kubernetes Probe (`tcpSocket`/`httpGet` target, `initialDelaySeconds`, `periodSeconds`, `failureThreshold` — the CRD's `spec.probes` has no A2S/UDP-ping/protocol-aware probe type) across the existing modules `modules/cs2/template.yaml`, `modules/palworld/template.yaml`, `modules/rust/template.yaml`, `modules/dayz/template.yaml`, `modules/garrys-mod/template.yaml`, `modules/ark-survival-ascended/template.yaml`, `modules/valheim/template.yaml` against `specs/015-top-steam-game-modules/engine-matrix-resolved.md`. Wire-protocol correctness (A2S_INFO, query responses) for these games is verified by their existing `test/e2e/internal/<game>/` probe packages, not by this task.
- [ ] T114 [US5] Audit `spec.probes.readiness` as a plain Kubernetes Probe (same scope as T113) across `modules/terraria/template.yaml`, `modules/factorio/template.yaml`, `modules/7-days-to-die/template.yaml`. Wire-protocol correctness (Terraria custom TCP, Factorio custom UDP ping, 7-days-to-die's query protocol) is verified by their existing `test/e2e/internal/<game>/` probe packages, not by this task.
- [ ] T115 [US5] Audit `spec.probes.readiness` as a plain Kubernetes Probe (same scope as T113) for `modules/project-zomboid/template.yaml`, `modules/dont-starve-together/template.yaml`, and `modules/satisfactory/template.yaml`. Wire-protocol correctness (including satisfactory's HTTPS API) is verified by the existing `test/e2e/internal/<game>/` probe packages, not by this task.

**Checkpoint**: all 26 modules have a real protocol-aware health probe (existing 13 already had one; the 13 new modules get one in Phase 2 plus a bot test here), and the CRD's `spec.probes` Kubernetes-Probe configuration is verified separately from wire-protocol correctness. All five user stories are independently functional, and Constitution Principle I's probe-trust rule is satisfied for every new game.

---

## Phase 8: Polish & Cross-Cutting Concerns

**Purpose**: Documentation (including the CI-gating game-coverage table), version-catalog pinning, validation, and the cross-repo submodule/PR mechanics that land the feature.

- [ ] T116 [P] Add one row per new module (`fivem`, `team-fortress-2`, `farming-simulator-25`, `euro-truck-simulator-2`, `mount-and-blade-2-bannerlord`, `tmodloader`, `beammp`, `left-4-dead-2`, `the-isle`, `ark-survival-evolved`, `arma-reforger`, `hell-let-loose`, `squad`) to `docs/game-coverage.md`'s first table (Module | Game | Status | Depth | Test | Bucket | Last Verified | Blocker | Blocker Class), satisfying `test/e2e/joincoverage.sh` checks 2 (module-in-`modules/`-must-be-listed — the CI job `e2e bucket coverage`, `.github/workflows/ci.yaml:775`, hard-fails without this), 4 (no stray modules in coverage table — listed but not in `modules/`), 5 (status/depth consistency), 8 (covered-has-bucket), 10 (covered-in-CI cannot use `bucket_bot_heavy`), 11 (covered-has-last-verified), 12/13 (blocked-doc must name a concrete artifact — "undocumented", "wire format", "reverse-engineer", …), 14 (out-of-scope-by-design needs an architectural blocker), 15 (Test+Bucket must match `test/e2e/buckets.sh`) and 16 (bot test names must agree with depth). Depends on T075, T082, T095, T112, T125 (the bucket-registration tasks whose Test/Bucket values this table must match, and the submodule-pointer bump that puts the 13 new module directories on this branch before the table references them).
- [ ] T117 [P] Cross-link the new catalog from `docs/module-authoring.md`, adding the 13 new module names to whatever module inventory list already exists there.
- [ ] T118 [P] Add an `### Added` entry to `CHANGELOG.md`'s Unreleased section recording the two new `spec.rcon.protocol` values `rest` and `cli` (additive, not breaking) and the four Gameplane-owned module images, matching the phrasing of the existing enum entries at `CHANGELOG.md:209-222`. Depends on T016, T031.
- [ ] T119 Author `spec.versions` (id / displayName / image, exactly one `default: true`) for the 13 new modules (`fivem`, `team-fortress-2`, `farming-simulator-25`, `euro-truck-simulator-2`, `mount-and-blade-2-bannerlord`, `tmodloader`, `beammp`, `left-4-dead-2`, `the-isle`, `ark-survival-evolved`, `arma-reforger`, `hell-let-loose`, `squad`) with `@sha256:` digest-pinned images — the four Gameplane-owned images (`fivem`, `farming-simulator-25`, `euro-truck-simulator-2`, `beammp`) are pinned from T031's build/push/sign output, the other 9 from their community upstreams — satisfying FR-003 and the data-model `VersionCatalog` entity; run `make module-pin` (Makefile:304-312) to re-pin, reading its diff warning before committing ("Each changed digest is a game binary that changes for every server on its next restart -- read the diff before committing") — this is the only task in the file permitted to use `--pin` — and confirm rule 10 (images pinned) and rule 5 (image resolves) are clean. Not `[P]`: rewrites the same `template.yaml` files Phase 3's `[P]` module tasks own. Depends on T031, T047, T050, T052, T053, T055, T058, T059, T061, T063, T067, T068, T069, T070.
- [ ] T120 Run `python3 modules/validate.py` locally in its default, read-only mode (do NOT pass `--pin` — see T119, the only task permitted to). It validates every directory under `modules/` (17 today, 30 after this feature, including `enshrouded`, `minecraft-java`, `nuclear-option`, `v-rising` — outside this feature's 26); fix every reported error and unacknowledged warning in the 26 modules this feature owns until they are 0/0 per SC-002, and leave the other modules unchanged unless a regression is introduced here. The four modules outside this feature's 26 (`enshrouded`, `minecraft-java`, `nuclear-option`, `v-rising`) are excluded from T013's new layout-ERROR rule by its allowlist, so this run does not need them to pass it. Depends on T119.
- [ ] T121 Verify the numbering-fix tasks (T006-T010) landed cleanly repo-wide: `grep -rIn "014-top-steam-game-modules" --exclude-dir=.git .` returns zero hits. This replaces the earlier plan to record the mismatch as a documentation debt — the maintainer has settled it (`015` stands) and T006-T010 already applied the fix; this task is verification only. Depends on T006, T007, T008, T009, T010, T026.
- [ ] T122 Commit and push the `modules/` submodule branch created in T002 (including T119's `--pin` re-pin and T120's clean validator run, T013's `test-validate-py.sh` check, T017's regenerated schema, and T027-T031's four new images, all now registered in that repo's own CI) so the `gameplane-module` repo's own CI runs its suites. Depends on T002, T013, T017, T031, T119, T120.
- [ ] T123 Push this repo's branch (`claude/top-steam-game-modules-x7oyy1`, containing T005-T011's docs and artifact fixes, T016/T020's CRD+agent changes, (T022's validator sync lands in the `gameplane-module` repo via T122 and reaches this branch only as T125's submodule-pointer bump), T116/T117's doc updates, T121's numbering verification, and every Phase 2-7 change) so GitHub Actions runs `make test`/`make lint`/`make cover`/the e2e buckets; do not run any of these locally (CLAUDE.md rule 8) — anchor file `.github/workflows/ci.yaml`. Depends on T116, T121, T125.
- [ ] T124 Open the PR in `gameplane-module` for the branch from T002/T122, referencing this feature's spec folder in the description. Depends on T122. — `modules/`.
- [ ] T125 Once the maintainer merges the `gameplane-module` PR, bump the `modules` submodule pointer in this repo (`git add modules && git commit`) and push it to the branch from T123. Depends on T124. — `modules` (submodule gitlink).
- [ ] T126 Verify the CI run on this repo's branch is green (`gh run watch` / `gh run list --branch claude/top-steam-game-modules-x7oyy1`, checked against the branch directly — before any PR is opened), including the `e2e bucket coverage` job (which now also validates against `docs/game-coverage.md` per T116) — fix any failures with follow-up commits on the branch rather than force-pushing over history. Depends on T123, T116, T125.
- [ ] T127 Once T126 confirms the branch is green, open this repo's PR and apply the widened label set this feature's scope now requires — `type: feature` plus `area: modules`, `area: operator`, `area: agent`, `area: chart`, `area: web`, `area: e2e`, `area: specs` (the CRD/agent/RCON-protocol work in Phase 2 touches `operator/`, `agent/`, `charts/`, and `web/` in addition to `modules/`, `specs/`, and `test/e2e/`; the enum addition is additive, not breaking, so no `breaking` label) — via `gh api -X POST repos/ValgulNecron/Gameplane/issues/<n>/labels -f "labels[]=type: feature" -f "labels[]=area: modules" -f "labels[]=area: operator" -f "labels[]=area: agent" -f "labels[]=area: chart" -f "labels[]=area: web" -f "labels[]=area: e2e" -f "labels[]=area: specs"` — `gh pr edit` is broken on this repo per CLAUDE.md rule 14, so this REST call is mandatory, not optional. Open the PR only once CI is green: `dismiss_stale_reviews_on_push` (CLAUDE.md rule 12) drops any approval if a fixup commit lands afterward. Depends on T126.
- [ ] T128 Once the maintainer approves and merges this repo's PR (T127), create a fresh branch off `master` for the spec-folder rename of `specs/015-top-steam-game-modules/` — the merged feature branch is deleted in T131 and cannot be reused. Depends on T127.
- [ ] T129 On the branch from T128, rename `specs/015-top-steam-game-modules/` to `specs/done_015-top-steam-game-modules/` (`git mv`) and fix every in-repo reference to the old path in the same commit (`grep -rIn "specs/015-top-steam-game-modules" --exclude-dir=.git .`), as a single signed `docs: mark feature 015 complete` commit, per CLAUDE.md rule 16. Depends on T128.
- [ ] T130 Open a PR for the T129 commit and apply labels `type: docs`, `area: specs` via the `gh api` REST route (`gh pr edit` is broken per CLAUDE.md rule 14). Depends on T129.
- [ ] T131 After the maintainer approves and merges the T130 rename PR, delete both branches, remote and local: the original feature branch (`claude/top-steam-game-modules-x7oyy1`) and the rename branch created in T128, per CLAUDE.md rule 12 — anchor `specs/done_015-top-steam-game-modules/tasks.md`. This is the final task in the file. Depends on T130.

**Checkpoint**: game-coverage table and module-authoring catalog documented, version catalogs pinned, validator clean, both repos' CI green, PR merged and labeled, spec folder renamed `done_`, both branches cleaned up.

---

## Dependencies & Execution Order

### Phase Dependencies

- **Setup (Phase 1)**: no dependencies.
- **Foundational (Phase 2)**: depends on Setup; BLOCKS every user story (Phase 3-7). Beyond the per-module prep work (T005-T015, including T012's mount-path divergence findings), it implements `rest`/`cli` end-to-end (T016-T025: CRD enum → `make generate`/`make manifests` → `make module-schema` → agent clients → agent dispatch → `agent/specs.md` sync → validator sync → `docs/module-authoring.md` sync → web audit → operator `helpers.go`/`gameserver_rcon.go` application) and builds the four Gameplane-owned images (T026-T031: research.md amendment → four Dockerfile+entrypoint tasks → shared build/push/sign pipeline). Includes 13 `[P]` new-game probe packages (T032-T044), each individually depended on by the Phase 4-7 test tasks for that game — see each test task's own "Depends on" line.
- **User Story 1 (Phase 3)**: depends on Foundational only (specifically T011's resolved matrix and T014's specs skeleton). Two module tasks have additional Foundational dependencies because their admin surface uses the new `rest` protocol or a Gameplane-owned image: `fivem` (T047) depends on T016/T020/T027; `farming-simulator-25` (T052) depends on T016/T020/T028; `euro-truck-simulator-2` (T053) and `beammp` (T059) depend on their respective image tasks (T029/T030) but not on the RCON-protocol chain, since neither uses `rest`. No module task adopts `cli` — its semantics are still open (T005). No dependency on any other story.
- **User Stories 2-5 (Phases 4-7)**: each depends on Foundational **and** on User Story 1 (Phase 3) having authored the `template.yaml` files they audit and test — this is a file-ownership dependency, not a feature dependency, and it is the reason these phases are numbered after Phase 3 even though US2 shares US1's P1 priority. Each new-game test task additionally depends on that game's Phase 2 probe package. Stories 2-5 remain independently testable from each other (spec.md's per-story Independent Test criteria hold once Phase 3 is done, with SC-005/SC-006 covered by the stated sampling); they run in priority order here (P1 → P2 → P2 → P3) purely to give the earliest checkpoint the most value, not because US3 needs US2 or similar.
- **Polish (Phase 8)**: depends on all desired user stories being complete.

### Within Each User-Story Phase

- E2E test-authoring tasks (new files) are `[P]` — genuinely independent of each other and of the audit tasks, but each new-game test task depends on that game's Phase 2 probe package (T032-T044).
- Bucket-registration tasks are never `[P]` (they all write the single shared `test/e2e/buckets.sh`) and always depend on the test tasks they register.
- Audit/fix tasks that touch a `template.yaml` already authored in Phase 3 are never `[P]` — they are safe by phase ordering (Phase 3 always precedes), but marking them `[P]` against each other would risk two tasks editing the same file if the plan were ever parallelized across phases, so they are sequenced instead.
- Every new e2e test task, and every existing one modelled on, MUST call `t.Parallel()`, use per-test unique resource names, and respect the shared-state guards `ociPushMu` / `ensureResticRepo` (CLAUDE.md e2e conventions; Constitution Principle I). Model on `test/e2e/cs2_bot_e2e_test.go` and `test/e2e/gamebot_helpers_e2e_test.go`; load the `e2e-test-authoring` skill before starting any test-authoring task.

### Parallel Opportunities

- All `[P]` Setup tasks (T002-T004) together.
- Within Foundational, once T001 (submodule init) is done: the documentation/artifact-correction group T005-T011, T013, T014 (all `[P]`, disjoint files) together; T012 is not `[P]` even though it sits in that range — it depends on T005 and appends to the same `OPEN-DECISIONS.md` file T005 creates, so it is sequenced after T005 rather than run concurrently with it; the RCON-protocol-chain tasks T016, T018, T024 are `[P]` with that group and with each other (T017 depends on T016, T019 depends on T005, T020 depends on T018/T019, T021 depends on T018/T019/T020, T022 depends on T013, T023 shares `docs/module-authoring.md` with T117 so it is not `[P]`, T025 depends on T005 — none of these seven are `[P]`); the image-build group T026-T030 is `[P]` with everything above (T031 depends on T027-T030, not `[P]`); the 13 probe packages T032-T044 remain `[P]`. T015 (gamebot helpers) is not `[P]`.
- 25 of the 26 `[P] [US1]` module tasks in Phase 3 (T045-T066, T068-T070) together — each owns its own `modules/<name>/` directory. T067 (ARK: Survival Evolved) is not `[P]`: it depends on T060 (ARK: Survival Ascended) for cluster-travel layout consistency.
- Within each of Phases 4-7, all `[P]`-tagged e2e test tasks together (respecting each one's probe-package dependency from Phase 2).

---

## Parallel Example: User Story 1

```bash
# Any subset of the 25 [P] module tasks can run together — each is confined to
# its own modules/<name>/ directory. T067 (ark-survival-evolved) is excluded:
# it depends on T060 and is not [P].
Task: "T045 [P] [US1] Standardize modules/cs2/"
Task: "T047 [P] [US1] Create modules/fivem/"
Task: "T050 [P] [US1] Create modules/team-fortress-2/"
Task: "T070 [P] [US1] Create modules/squad/"
```

## Parallel Example: User Story 4

```bash
Task: "T093 [P] [US4] test/e2e/tmodloader_mods_e2e_test.go"
Task: "T094 [P] [US4] test/e2e/garrysmod_workshop_e2e_test.go"
```

## Parallel Example: User Story 5

```bash
Task: "T099 [P] [US5] test/e2e/fivem_bot_e2e_test.go"
Task: "T100 [P] [US5] test/e2e/teamfortress2_bot_e2e_test.go"
Task: "T108 [P] [US5] test/e2e/arksurvivalevolved_bot_e2e_test.go"
Task: "T111 [P] [US5] test/e2e/squad_bot_e2e_test.go"
```

---

## Implementation Strategy

### MVP First (Setup + Foundational + User Story 1)

1. Complete Phase 1 (Setup).
2. Complete Phase 2 (Foundational) — CRITICAL, blocks everything else; includes correcting the spec artifacts, implementing `rest`/`cli` end-to-end, building the four Gameplane-owned images, and proving all 13 new-game probe packages against a dead address and a real listener.
3. Complete Phase 3 (User Story 1) — all 26 modules deployable.
4. **STOP and VALIDATE**: run `python3 modules/validate.py` locally in its default mode (T120's check, pulled forward), confirm 0 errors; push the branch and let CI's e2e buckets confirm the 13 already-existing bot tests still pass against the standardized templates.
5. This is the deployable MVP — every module in FR-001 exists and boots.

### Incremental Delivery

1. Setup + Foundational → foundation ready, `rest`/`cli` implemented, Gameplane-owned images built, all new-game probes proven.
2. + User Story 1 → all 26 modules deployable (MVP).
3. + User Story 2 → persistence and multi-shard clustering verified (sampled, SC-005).
4. + User Story 3 → RCON/admin and graceful shutdown verified (sampled, SC-006).
5. + User Story 4 → modding/workshop paths verified.
6. + User Story 5 → health probing verified for all 26 (Constitution Principle I fully satisfied).
7. Polish → game-coverage table published, version catalogs pinned, both repos' PRs merged, labeled, branches cleaned, spec folder renamed `done_`.

---

## Notes

- `[P]` tasks touch different files and carry no dependency on another incomplete task — safe to hand to concurrent subagents per CLAUDE.md rule 13's smallest-model-first, fan-out-in-one-Workflow guidance; start every module-authoring, probe-package-authoring, Dockerfile-authoring, and e2e-test-authoring task at `haiku`, escalating only on demonstrated failure, and review each wave one tier up before merging its fixes.
- `modules/` is a git submodule pointing at `ValgulNecron/gameplane-module` — every `modules/<name>/` edit in Phases 2-7 (including the four new Dockerfiles) is committed in that repo, not this one; this repo only ever records the bumped submodule pointer (T125).
- Never run `make test`, `make lint`, `make cover`, `go test`, or `npm test` locally — GitHub Actions is authoritative (CLAUDE.md rule 8). `python3 modules/validate.py` in its default mode (T120) is the sole sanctioned local check besides codegen; `--pin` mode (T119) rewrites templates and is not a read-only preflight. `make generate`, `make manifests` (T016) and `make module-schema` (T017) are codegen, not test suites, and CLAUDE.md rule 7 requires them locally in the same commit as the CRD Go type edit that triggers them.
- CRD Go types ARE touched by this feature: T016 adds `rest` and `cli` to `spec.rcon.protocol`'s enum in `operator/api/v1alpha1/gametemplate_types.go`, paired in the same commit with `make generate && make manifests` per CLAUDE.md rule 7.
- A new e2e test that is not registered in `test/e2e/buckets.sh` fails the `e2e bucket coverage` CI job — every test-authoring task above has a paired registration task; do not skip it. That same CI job also fails on any `modules/` directory missing from `docs/game-coverage.md` (T116) — a module can be schema-valid and still fail CI if this table is stale.
- Heavy games get their e2e test **committed and bucketed**, with an explicit CI-exclusion comment in `test/e2e/buckets.sh` (matching the existing `bucket_bot_heavy` block's per-test justification comments) — never a silent skip, per spec.md Edge Cases and Constitution Principle I. `bucket_bot_heavy` itself is never executed by any CI job today.
- Every new e2e test MUST call `t.Parallel()`, use per-test unique resource names, and respect `ociPushMu` / `ensureResticRepo` (CLAUDE.md e2e conventions; Constitution Principle I). Model on `test/e2e/cs2_bot_e2e_test.go` and `test/e2e/gamebot_helpers_e2e_test.go`; load the `e2e-test-authoring` skill before starting any test-authoring task in this file.
- `agent/internal/rcon/rest.go` and `cli.go` (T018/T019) need real Go unit tests, not just wiring: `agent/.testcoverage.yml` gates the `agent` module at 90% and these are new, uncovered files.
- Two questions remain genuinely open after every other artifact/contract mismatch this feature found was ruled on: `cli`'s exact semantics (T005/T019) and whether `factorio`/`project-zomboid`'s shipped `rcon.protocol: source` or the (now-corrected-elsewhere) contract's claim is the bug (T005). Neither blocks the MVP — no module task in Phase 3 adopts `cli`, and factorio/project-zomboid are verified as-shipped, not changed.

---

## Summary Statistics

- **Total tasks**: 131 (T001-T131)
- **Phase 1 (Setup)**: 4 tasks (T001-T004)
- **Phase 2 (Foundational)**: 40 tasks (T005-T044) — 30 `[P]`; includes 5 direct artifact-correction tasks (T006-T010), a mount-path divergence findings task (T012, not `[P]`: depends on and shares a file with T005), the `rest`/`cli` protocol implementation chain (T016-T025, of which T023 is not `[P]`: shares `docs/module-authoring.md` with Phase 8's T117), 4 Gameplane-owned-image tasks plus a shared build/push/sign task (T026-T031), and 13 `[P]` new-game probe packages (T032-T044)
- **Phase 3 (US1)**: 28 tasks (T045-T072) — 25 `[P] [US1]` module tasks (T067 is not `[P]`) plus 2 non-`[P]` `[US1]` security audits (T071, T072)
- **Phase 4 (US2)**: 7 tasks (T073-T079) — 2 `[P]`
- **Phase 5 (US3)**: 13 tasks (T080-T092) — 2 `[P]`
- **Phase 6 (US4)**: 6 tasks (T093-T098) — 2 `[P]`
- **Phase 7 (US5)**: 17 tasks (T099-T115) — 13 `[P]`
- **Phase 8 (Polish)**: 16 tasks (T116-T131) — 3 `[P]`
- **New modules authored**: 13 (`fivem`, `team-fortress-2`, `farming-simulator-25`, `euro-truck-simulator-2`, `mount-and-blade-2-bannerlord`, `tmodloader`, `beammp`, `left-4-dead-2`, `the-isle`, `ark-survival-evolved`, `arma-reforger`, `hell-let-loose`, `squad`)
- **Existing modules verified/updated**: 13 (`cs2`, `palworld`, `rust`, `project-zomboid`, `dayz`, `garrys-mod`, `terraria`, `7-days-to-die`, `ark-survival-ascended`, `factorio`, `dont-starve-together`, `valheim`, `satisfactory`)
- **New `samples/gameserver.yaml` authored**: 7 (`project-zomboid`, `dayz`, `garrys-mod`, `7-days-to-die`, `ark-survival-ascended`, `dont-starve-together`, `satisfactory`)
- **New e2e wire-protocol probe packages authored**: 13 (`test/e2e/internal/<game>/app.go` + `spec.md`, one per new module — T032-T044)
- **New e2e test files authored**: 19 (T073, T074, T080, T081, T093, T094, T099-T111)
- **New `rcon.protocol` values implemented end-to-end**: 2 (`rest`, `cli` — T016-T025: CRD enum, codegen, agent clients, agent dispatch, `agent/specs.md` sync, validator sync, `docs/module-authoring.md` sync, dashboard audit, operator `helpers.go`/`gameserver_rcon.go` application)
- **New Gameplane-owned container images**: 4 (`fivem`, `farming-simulator-25`, `euro-truck-simulator-2`, `beammp` — T026-T031)
- **Spec-artifact correction tasks (maintainer-authorized, no longer deferred)**: 5 (T006-T010)
- **Mount-path divergence findings task**: 1 (T012, recording per-module findings in `OPEN-DECISIONS.md` for shipped/contract `storage.mountPath` mismatches)
