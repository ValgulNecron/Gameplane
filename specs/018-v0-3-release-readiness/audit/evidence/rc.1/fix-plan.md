# Fix Plan (T050)

Scope: `specs/018-v0-3-release-readiness/audit/findings.md` only (the git-bound findings; security findings held off-git under [OD-019](../../OPEN-DECISIONS.md#od-019-security-findings-stay-unpushed-until-fixed--resolved-2026-09-24) are out of scope for this plan and are not mentioned below).

This plan **proposes** groupings, branches, and closures. It changes no finding's status and no other record. All `not-a-defect` / `out-of-scope` closures below are marked `PROPOSED` and need the maintainer's sign-off (CLAUDE.md override 1) before T050 applies them. Fix execution (T055) follows this plan's order and starts only after the maintainer's sign-off in OD-024 (pending), since every fix branch changes production code and adds tests.

## Summary

| | Count |
|---|---|
| Findings considered (imported + open + fixing) | 199 |
| — `imported` (→ Re-check first) | 25 |
| — `fixing` (→ In flight) | 4 |
| — `open` (→ Fix groups) | 170 |
| Open findings by severity | S1: 3 · S2: 6 · S3: 53 · S4: 108 |
| Fix groups / branches proposed | 52 |
| Proposed closures | 0 |

No open finding met the bar for `not-a-defect` (a written justification that the observed behaviour is correct) or `out-of-scope` (a `docs/roadmap.md` citation to a capability placed after v0.3) on the evidence read for this plan. Every open finding therefore appears in a fix group below. If a fix agent's scout brief turns up such a justification while working a group, it goes back to the maintainer as a new proposed closure, not a silent status change.

---

## In flight

Already `fixing`, with an open PR. Not replanned here; T056 watches their CI and fixes failures. Each PR merges only after a human approval (CLAUDE.md rule 12: agents cannot merge to `master`), and T056 then sets the finding to `fixed-unverified`.

| ID | Title | Component | Severity | PR |
|----|-------|-----------|----------|----|
| F-026 | `hack/check-doc-versions.sh` recognises only `-beta.N` versions, single-digit `[0-9]` | hack/ | S3 | #420 |
| F-029 | CI upgrade baseline still `0.2.0-beta.5` | deploy/ | S3 | #422 |
| F-030 | CLAUDE.md repo map omits `gp-module/`, says "14 Go modules" | root docs | S4 | #421 |
| F-043 | CHANGELOG.md "Unreleased" missing ~60 PRs | root docs | S4 | #423 |

---

## Re-check first (T049)

Every `imported` finding (F-001–F-025) needs T049's live repro on rc.1 before it can be planned as a fix. Most carry an `Actual` note that the fix already merged pre-audit; T049 either confirms that live and sets `closed-already-fixed`, or finds it still broken and sets `open` (in which case it re-enters this plan on the next round via T059). None are re-planned here. Pending decisions that touch this lane: OD-022 decides the component rows for F-006 and F-013 (`.github/actions/`) and for F-012 and F-017 (`images/`), so if any of them turns `open`, its fix group's component and `area:` label wait on OD-022; F-020's live check needs the `nuclear-option` Module healthy, which OD-021 item 3 questions.

| ID | Title | Believed state per findings.md | T049 needs to confirm |
|----|-------|-------------------------------|------------------------|
| F-001 | Dependency bump (#406) broke upload-dialog tests / keyboard-focus e2e | fixed in #406 | `web` + `web e2e (mock)` green on rc.1 lineage |
| F-002 | Visual-diff residuals, 7 screens | fixed in #378 | visual-diff job green |
| F-003 | Design exports mismatched shipped UI | fixed in #378 | exports match `design-export/` |
| F-004 | `page.route()` silently no-ops under mock-target MSW | fixed in #378 | named specs use variant cookies |
| F-005 | Share-links e2e: missing `/shares` proxy rule | fixed in #371 (dev/CI only) | `vite.config.ts:59` still carries the rule |
| F-006 | `dump-cluster-state` misses quoted/JSON secret values | fixed in #327 | redact() pattern still present |
| F-007 | Console WS 403 on non-default ports | fixed in #416 | non-default-port console still connects |
| F-008 | Module catalog tag filters unioned not intersected | fixed in #411 | multi-tag filter narrows correctly |
| F-009 | Vertical tab list rendered as oval | fixed in #410 | visual check |
| F-010 | Console terminal grew without bound | fixed in #409 | terminal height stable over time |
| F-011 | Preset share links didn't expire end-of-day | fixed in #396 | preset expiry lands at 23:59:59 |
| F-012 | Security-audit hardening bundle (RBAC/share/tunnel/GameServer validation) | fixed in #350 | controls present; live confirm |
| F-013 | `dump-cluster-state` redaction fix (pairs with F-006) | fixed in #327 | same as F-006 |
| F-014 | Share links not cluster-scoped | fixed in #350 | cross-cluster resolve/list/revoke refused |
| F-015 | GameServer env Secret/ConfigMap refs not ownership-checked | fixed in #350 | non-owned ref rejected live |
| F-016 | Audit-syslog-bridge had no ingress NetworkPolicy | fixed in #350 | policy renders and applies |
| F-017 | steamcmd base image on mutable `:latest` | fixed in #350 (digest since rebumped by #380) | `FROM` still `@sha256:` |
| F-018 | Tunnel credential Secret ops lacked ownership checks | fixed in #350 | non-owned Secret delete/put refused |
| F-019 | Telemetry-receiver had no ingress NetworkPolicy | fixed in #350 | policy renders and applies |
| F-020 | Nuclear Option UDP 7777 not bound | believed **not-a-defect** (matches FR-004's documented contingency) | T049 confirms the belief; if it holds, T050's next round proposes the closure — not assumed here |
| F-021 | OIDC Helm role mappings under CHANGELOG Unreleased | believed fixed by #340 | `docs/oidc.md:21` carries the qualifier |
| F-022 | `cli` rcon.protocol semantics vs Option A ruling | unresolved | operator `resolveRCON`, agent, `rconAvailable` match the ruling |
| F-023 | Factorio `rcon.protocol: source` vs earlier `none` claim | unresolved | maintainer confirmation |
| F-024 | Project Zomboid `rcon.protocol: source` vs earlier `none` claim | unresolved | maintainer confirmation |
| F-025 | Generic `rest` rcon wire contract not finally ruled on | implemented in `agent/internal/rcon/rest.go` | maintainer sign-off |

---

## Fix groups (in order)

Ordered S1 → S4; within a severity, core paths come first in this order: install/upgrade, auth, game-server lifecycle, backup/restore, console. Other code groups follow, and docs-only groups come last. A group that absorbs a lower-severity twin (same files, same root cause) sits at its highest finding's severity, and the lower severity is shown next to the twin's ID. Branch names are proposals for T055's fan-out. T055 names branches `fix/018-F-NNN` (one per finding) while T050 asks for one branch per group; the names below follow T050, and T054/T063 keep the branch names those tasks set. The naming conflict is raised with OD-024. `type:`/`area:` labels per CLAUDE.md rule 14. "specs.md?" = does the owning component's `specs.md` need an update alongside the code (CLAUDE.md common workflow / T055). "design.pen?" = does a web UI change need a Pencil pass first (CLAUDE.md rule 1) before any React edit.

Regression-test conventions used below:
- Go E2E cases name a real bucket in `test/e2e/buckets.sh`. New cases in `api-*` buckets must fit that bucket's admin-login budget (about 7 per job, per the `buckets.sh` header; `api-roles` is already there, see F-042); prefer extending an existing test. `bot-heavy` never runs in CI, so it cannot carry a regression.
- The dashboard is not driven by any `buckets.sh` bucket. Web fixes use the Playwright suites instead: `web/e2e/specs/*.spec.ts` (CI job `web e2e (mock)`; mock specs pick MSW handler variants by cookie, because `page.route()` does not intercept under MSW, see F-004) or `web/e2e/specs/live/*.spec.ts` (job `e2e web live`, which reaches the API through the Vite proxy, not the web pod's nginx).
- Chart fixes can add render assertions to the `chart render` job in `.github/workflows/ci.yaml`.
- Any new or changed test, fixture or CI check needs the maintainer's sign-off first (CLAUDE.md override 1).
- Live re-verification (T058) of some groups depends on pending OD-021 items; the Dependencies column names them.

### Severity S1 — data loss / security-boundary / upgrade-rollback corruption

| # | Branch | Labels | Findings | Files likely touched | Regression test | specs.md? | design.pen? | Dependencies |
|---|--------|--------|----------|----------------------|------------------|-----------|-------------|--------------|
| 1 | `fix/018-charts-games-namespace-keep` | `type: fix`, `area: chart` | F-212 (S1) `helm uninstall` deletes the games Namespace and every GameServer/PVC in it | `charts/gameplane/templates/namespaces.yaml` (add `helm.sh/resource-policy: keep` to the games Namespace, or an explicit uninstall guard), `docs/install.md:544-546`, `charts/gameplane/values.yaml:30-31` | Render check in the `chart render` CI job asserting the games Namespace carries `helm.sh/resource-policy: keep`. A live `helm uninstall` case must not go in the `operator` bucket: it would delete state that every parallel test there shares. If a live case is wanted, run it as a last, non-parallel step in the `upgrade` job (its own cluster) or as a new `buckets.sh` bucket with its own CI job | no `specs.md` (the chart has none); `docs/install.md:544-546` and `values.yaml:30-31` already promise that servers survive, so they change only if the fix is a warning instead of a keep policy | no | T061 (upgrade round) is blocked on OD-023 whichever option the maintainer picks. Only OD-023 option (e) waits for this group to ship in an RC; options (a), (c) and (d) do not. Until OD-023 is decided, `procedures/upgrade.md` baseline-beta8 step 3 keeps its DO NOT RUN banner |
| 2 | `fix/018-agent-file-write-safety` | `type: fix`, `area: agent` | F-102 (S1) write/upload truncates or deletes on error; F-108 (S3) delete follows symlinks | `agent/internal/files/files.go` (`write`, `savePart`, `resolve`, `del`) | Unit: `agent/internal/files/files_test.go`: write-to-temp-then-rename leaves the original intact on an injected mid-copy error; delete of an in-root symlink removes the link, not its target. The file API can't create a symlink, so an `api-agent` E2E can't set up F-108 | no (agent behaviour matches spec's intent; confirm no `specs.md` promise contradicts atomic-write) | no | none |
| 3 | `fix/018-web-file-overwrite-guard` | `type: fix`, `area: web` | F-115 (S1) "New file" silently truncates an existing file | `web/src/routes/tabs/Files.tsx` (new-file dialog), `web/src/lib/api.ts` | E2E: `web/e2e/specs/fileManager.spec.ts` (`web e2e (mock)`): create a file whose name is already in the current listing and assert a block/confirm, not a silent empty write | no | **yes** — new confirm-before-overwrite affordance is a new UI state | depends on group 2 only for shared root cause awareness (agent still truncates on write; the web guard should stop the request before it's sent) |

### Severity S2 — core path broken, no workaround

| # | Branch | Labels | Findings | Files likely touched | Regression test | specs.md? | design.pen? | Dependencies |
|---|--------|--------|----------|----------------------|------------------|-----------|-------------|--------------|
| 4 | `fix/018-web-namespace-scoping` | `type: fix`, `area: web` | F-116 (S2) multi-namespace server actions (Mods, Backups, Logs, Quick actions, Settings, Clone) 404 because `ns` is dropped from mutation calls; F-130 (S4) rows under "Shared with you" on `/servers` have no actions because the page's `act` mutation calls `Servers.lifecycle` without `ns` (same root cause) | `web/src/routes/ServerDetail.tsx` and its tab components, `web/src/routes/Servers.tsx` (`act`, the row-actions guard), `web/src/lib/api.ts` | E2E: `web/e2e/specs/serverDetail.spec.ts` (`web e2e (mock)`) with a server opened as `?ns=team-a`, asserting Mods install, Back up now and Clone requests carry `namespace=team-a`; a `/servers` case asserting a shared row gets Start/Stop/Restart. (The `multicluster` bucket tests a second cluster, not a second namespace, and no bucket drives the dashboard) | no | no — plumbing fix; F-130's row actions reuse the existing buttons and menu | Live re-check (T058) needs a second games namespace on kubelab: OD-021 item 14 |
| 5 | `fix/018-charts-backup-restore-egress` | `type: fix`, `area: chart`, `area: operator` | F-215 (S2) default-deny-egress NetworkPolicy blocks restic Backup/Restore Jobs from reaching any repository | `charts/gameplane/templates/networkpolicies.yaml`, `charts/gameplane/values.yaml`, `docs/install.md`; `operator/internal/controller/backup_controller.go` / `restore_controller.go` if the Job pods need a label for the policy to select (today they carry only the Job controller's labels) | E2E: `operator` bucket `TestBackup_OperatorMaterializesJob` and `TestRestore_RoundTrip` already run restic against `test/e2e/fixtures/restic-server.yaml`, whose `allow-egress-to-restic` policy (`:61-85`) works around this defect. The regression is those tests passing with that fixture policy narrowed or removed, so the chart's own policy carries the traffic (strengthens the test; still a fixture change) | yes — chart docs should name the destinations the default policy allows | no | Group 14 edits the same backup/restore controllers; whichever lands second rebases. Live re-check needs a restic repository on kubelab: OD-021 items 9 and 16 |
| 6 | `fix/018-web-nginx-upload-size` | `type: fix`, `area: web` | F-251 (S2) `nginx.conf.template` has no `client_max_body_size`, capping uploads below what the API/agent accept | `web/nginx.conf.template` | No CI job sends traffic through the web pod's nginx (the live Playwright job goes through the Vite proxy, and the Go suites call the API directly), so the regression is a config check, for example a `web` job step asserting `client_max_body_size` is set at or above the ingress's `64m`, plus a live dashboard upload over 1 MiB in T058 | no | no | related to group 16 (`api-timeout-bodylimit`) — same "large upload" user journey, different layer; no hard ordering dependency |
| 7 | `fix/018-web-tunnel-wizard-flow` | `type: fix`, `area: web` | F-118 (S2) create-with-tunnel always 403s (Secret doesn't exist yet); F-119 (S2) enabling a tunnel from Settings always fails CEL/patch validation | `web/src/routes/CreateServer.tsx` (Network step), `web/src/routes/tabs/settings/Networking.tsx`, `web/src/lib/api.ts` | E2E: `web/e2e/specs/live/` (`e2e web live`), because the failures come from the real API (403) and CRD (CEL) that the mock job doesn't model: one case creating a server with an frp tunnel enabled end-to-end, one enabling a tunnel on an existing server via Settings | no | **yes** — the fix likely reorders the wizard/settings flow (create disabled → save credential → enable, or a combined atomic call); confirm the sequence in `design.pen` before building it | none |
| 8 | `fix/018-tunnel-core-reliability` | `type: fix`, `area: optional-components`, `area: operator` | F-172 (S3) exponential backoff overflows to 0s after 64 retries; F-173 (S2) Tailscale config drops backing-service settings so nothing bridges tailnet traffic to the game; F-174 (S3) playit endpoint never written to `status.endpoints`; F-052 (S3) UDP tunnels forward as TCP because the frp config never reads the port protocol | `tunnel/main.go` (`exponentialBackoff`, `renderTailscaleConfig`, `renderFrpConfig`, `buildCommand`, the playit status-write TODO at `:282-285`), `operator/internal/controller/gameserver_tunnel.go` (the `BACKING_SERVICE_PORT` env it passes carries no protocol) | Unit: `tunnel` package tests for backoff bounds past 64 retries, a rendered Tailscale config that includes serve/forward rules, and `renderFrpConfig` emitting `type = "udp"` for a UDP port; an operator envtest asserting the port protocol reaches the tunnel Deployment's env. E2E is out of reach without real frps/Tailscale/playit endpoints, so cover what's unit-testable and note the live-account gap as blocked in the inventory | yes — `tunnel/specs.md` if it currently describes only the frp path as working | no | none |

### Severity S3 — degraded behaviour or a workaround exists

Core paths (install/upgrade, auth, lifecycle, backup/restore, console) first, then the rest, docs-heavy groups last.

| # | Branch | Labels | Findings | Files likely touched | Regression test | specs.md? | design.pen? | Dependencies |
|---|--------|--------|----------|----------------------|------------------|-----------|-------------|--------------|
| 9 | `fix/018-charts-upgrade-reliability` | `type: fix`, `area: chart` | F-213 (S3) the pre-upgrade hook targets `<release>-api` (`resourceNames` and the patch) while the chart always renders `gameplane-api`, so any non-default release name can never upgrade; F-214 (S3) `helm upgrade --reuse-values` from beta.8 fails to render on new nil-default keys (`capture.*`, `api.oidc.roleMappings`, `operator.gameDataStorage`); F-218 (S3) CRDs aren't updated when a newer chart is installed over CRDs an uninstall left behind, because the CRD sync hook is upgrade-only | `charts/gameplane/templates/crd-apply-hook.yaml` (hook Role `resourceNames` at `:98`; hook events for F-218), `charts/gameplane/templates/operator.yaml`, `api.yaml` (nil-safe reads) | Render checks in the `chart render` CI job: render with release name `gp` and assert the hook Role's `resourceNames` matches the rendered API Deployment; render the new chart with `v0.2.0-beta.8`'s `values.yaml` as values (the `--reuse-values` case) and assert it succeeds. E2E: `upgrade` bucket (its own job and cluster, not parallel): uninstall, then install the new chart over the previous release's leftover CRDs, asserting the CRD schema is current | yes — `docs/install.md` upgrade caveats | no | Feeds T061/OD-023: OD-023 notes that T015's rc deploy must pass the `capture` and `operator.gameDataStorage` keys explicitly because of F-214; an RC cut after this group merges no longer needs that workaround |
| 10 | `fix/018-operator-module-template-reconcile` | `type: fix`, `area: operator` | F-046 pinned module versions flap Pulling/Ready forever; F-047 `templateRef` change wedges the StatefulSet with only an operator-log error; F-050 a `kubectl`-deleted managed GameTemplate isn't recreated | `operator/internal/controller/module_controller.go`, `gameserver_controller.go` (templateRef reconcile), `gametemplate_controller.go`. If F-047 is fixed by making `templateRef` immutable at admission, that is a CRD change: `operator/api/v1alpha1/gameserver_types.go` plus `make generate && make manifests`, with the regenerated CRDs committed in the same changeset (CLAUDE.md rule 7) | E2E: `operator` bucket — `TestModuleSourceAndModule` variant pinning an older version and asserting convergence stops flapping; a `templateRef` immutability/CEL case; a managed-GameTemplate-delete-recreate case | yes — `operator/specs.md:29` (owned-object recreation) | no | none |
| 11 | `fix/018-operator-wipe-ack` | `type: fix`, `area: operator` | F-054 (S3) wipe Job always acks success even when `rm` failed (EACCES swallowed) | `operator/internal/controller/gameserver_wipe.go` (wipe script, `ackWipe`) | E2E: `operator` bucket — a wipe case with unwritable subdirectories asserting the Job/condition reports failure, not silent success | no | no | none |
| 12 | `fix/018-web-settings-lifecycle` | `type: fix`, `area: web` | F-117 (S3) Save overwrites the whole spec, silently reverting concurrent changes (e.g. a Stop while the form was dirty); F-122 (S3) dirty state and stale Overview persist after navigating away without saving; F-131 (S4) invalid Lifecycle/EnvVars/Resources values don't disable Save; F-132 (S4) Placement editors keep discarded text after Discard (all four are the Settings draft/save/discard lifecycle) | `web/src/routes/tabs/Settings.tsx` (draft, dirty, save and discard state), `web/src/routes/tabs/settings/{Lifecycle,EnvVars,Resources,Placement}.tsx` (`onValidityChange`, editor reset), save mutation in `web/src/lib/api.ts` | E2E: `web/e2e/specs/settingsSubTabs.spec.ts` (`web e2e (mock)`): dirty-then-navigate-away asserting polling resumes and no stale chip; an invalid "Sleep after" keeping Save disabled; Discard resetting the Placement editors. The concurrent-edit case (stop the server while the form is dirty, then save, asserting the stop isn't reverted) needs the real API: `web/e2e/specs/live/server-settings.spec.ts` (`e2e web live`) | no | **yes** — if the fix adds a "leave without saving?" prompt, that's new UI | none |
| 13 | `fix/018-web-server-create-placement` | `type: fix`, `area: web` | F-124 (S3) Pin-to-node/GPU-enabled on create sets a `nodeSelector` no node carries, so the pod stays Pending, and Settings → Placement has no way to remove it; F-127 (S3) Admin "Default namespace" setting is never read on server create | `web/src/routes/CreateServer.tsx`, `web/src/routes/tabs/settings/Placement.tsx`, the server-create submit path in `web/src/lib/api.ts` | E2E: `web/e2e/specs/createServer.spec.ts` (`web e2e (mock)`): a default-namespace-set-then-create case asserting `POST /servers` carries that namespace; a Pin-to-node case asserting the chosen fix (a removable selector, or the documented label) | no | **yes** for F-124 — Placement settings needs a "clear node selector" control (or the create option changes) | Live re-check of F-127 needs a second namespace on kubelab: OD-021 item 14 |
| 14 | `fix/018-operator-backup-quiesce-lifecycle` | `type: fix`, `area: operator` | F-044 terminal phase persisted before unquiesce lands; F-045 auto-scheduled backups skip the quiesce default; F-048 deleting a quiesced Backup never sends unquiesce; F-049 restore doesn't delete post-snapshot files | `operator/internal/controller/backup_controller.go`, `backupschedule_controller.go`, `restore_controller.go` | E2E: extend `operator` bucket — `TestBackup_OperatorMaterializesJob` variant asserting unquiesce-before-terminal-phase; `TestBackupSchedule_CreatesBackupCR` asserting `quiesce: true` default; a Backup-delete-during-quiesce case; `TestRestore_RoundTrip` variant asserting post-snapshot files are gone after restore | yes — `docs/architecture.md:154-158` (unquiesce timing, the contract F-044 cites) and the restore contract | no | Shares the backup/restore controllers with group 5 (F-215 Job pod labels), which lands first. Live re-check: quiesce is reachable only through a Backup (OD-021 item 2), and kubelab needs a restic repository (OD-021 items 9 and 16) |
| 15 | `fix/018-agent-stream-timeout-rcon` | `type: fix`, `area: agent` | F-103 (S3) router-wide 30s timeout drops Console/Logs WebSockets and mod-URL installs; F-104 (S3) RCON reply-size check rejects replies ≥4087 bytes (breaks long Minecraft `/players/banned` replies) | `agent/cmd/main.go:160` (exempt WS routes from `middleware.Timeout`), `agent/internal/rcon/rcon.go:300` (raise/remove the 4096 cap) | E2E: `api-agent` bucket (`TestAPI_ConsolePTYRoundTrip` and `TestAPI_LogsTailWS` already open these sockets): hold a console or logs session past 30s and assert no forced reconnect. Unit: `agent/internal/rcon` reading a reply packet with a size field ≥4106 whole (today `rcon_extra_test.go:65-81` covers only size 0) | yes — `agent/specs.md` RCON size bound | no | none |
| 16 | `fix/018-api-timeout-bodylimit` | `type: fix`, `area: api` | F-074 (S3) 60s requestTimeout cuts every non-WS/SSE transfer, including file downloads/uploads; F-075 (S3) global 1 MiB bodyLimit deadcaps the declared 512 MiB mod-upload / 64 MiB file-write limits | `api/cmd/main.go` (route middleware wiring around `:253`, `:602-623`, `:676-700`) | E2E: `api-agent` (`TestAPI_AgentFilesRoundTrip`) for a large-file download held open past 60s, and `api-mods` (`TestAPI_ModUpload`) for a multi-MB `/mods/upload` past 1 MiB, both asserting success instead of a cut connection / 502 | yes — `api/specs.md` middleware-exemption table if one exists | no | pairs with group 6 (web nginx limit) for the end-to-end upload path |
| 17 | `fix/018-api-clusterops-address` | `type: fix`, `area: api` | F-081 (S3) kubeconfig/join-command responses use the in-cluster ClusterIP, unreachable from outside | `api/internal/handlers/cluster.go`, `charts/gameplane/values.yaml` (configurable external API address) | Unit test on the handler with a configured external address override; no bucket covers `clusterOps`, so a unit-level regression is enough | yes — document the new config key | no | none |
| 18 | `fix/018-agent-status-reporting` | `type: fix`, `area: agent` | F-105 (S3) `gameVersion` reports the template id, not a version; F-106 (S3) unknown player-count state reports two different sentinel shapes (`-1/-1` vs `0/0`) instead of one documented "unknown" | `agent/internal/heartbeat/heartbeat.go:129`, the per-protocol players readers; `web/src/routes/tabs/Players.tsx` if the Players tab has to render the unknown value as "—" (then add `area: web`) | Unit: `heartbeat_test.go` fed a real version source instead of `cfg.Game`; a players-reader test asserting one unknown representation across both no-RCON and no-player-list modules | yes — `agent/specs.md` / `agent/openapi.yaml` "unknown" representation | no | none |
| 19 | `fix/018-web-share-links-reliability` | `type: fix`, `area: web`, `area: api` | F-120 (S3) share-page polling outpaces the rate limiter's refill, so a slow wake ends on "Link not available" while still valid; F-123 (S3) a revoked share link still shows "Active" in the owner's list | `web/src/routes/Share.tsx` (poll interval / 429 backoff), `web/src/routes/tabs/settings/ShareLinks.tsx` (status rendering); for F-123 the list response too (`api/internal/db/shares.go`, `api/internal/handlers/shares.go`), since the API returns the row with no revoked field | E2E: a mock-job case (MSW variant answering 429) asserting the share page stays on "waking up" through one or more 429s; `web/e2e/specs/live/share-links.spec.ts` (`e2e web live`) revoke-then-list asserting "Revoked" renders | no | possibly — if "Revoked" needs a new chip variant, check `design-export/json` first; likely reuses the existing Active/Expired chip set | Group 33 (F-082) edits the same `shares.go` files later; it rebases on this one |
| 20 | `fix/018-web-websocket-reconnect-leak` | `type: fix`, `area: web` | F-121 (S3) tab-switch during WS backoff leaks a reconnect timer, so an unmounted tab keeps opening sockets | `web/src/lib/ws.ts` (`close()`/`connect()` lifecycle) | Unit: `web/src/lib/ws.test.ts` — trigger a backoff window, call `close()`, and assert no further `WebSocket` construction via a spy/mock | no | no | none |
| 21 | `fix/018-web-safe-mode-persistence` | `type: fix`, `area: web` | F-125 (S3) `?safe-mode=1` is dropped by the very first in-app navigation (including the banner's own "Open Appearance Settings" link), re-enabling custom CSS | `web/src/lib/useThemePreferences.ts`, `web/src/components/ui/SafeModeBanner.tsx`, `web/src/routes/ThemeSettings.tsx` | Component test (`web/src/routes/ThemeSettings.test.tsx`) or `web/e2e/specs/live/theme-customization.spec.ts`: enter safe mode via URL, navigate in-app, assert custom CSS stays suspended until the user explicitly re-enables it | no | no | none |
| 22 | `fix/018-web-silent-failures` | `type: fix`, `area: web` | F-126 (S3) six mutations (viewer 403 on a Servers-row Start, duplicate role name, capture delete while the sidecar is unreachable) fail silently with no toast; F-137 (S4) four promise handlers are left unhandled, and Settings' Reload fails silently (same missing error handling) | mutation error handlers in `web/src/routes/Servers.tsx`, `web/src/routes/Users.tsx`, the capture delete dialog, and the handlers F-137 names; `web/eslint.config.js` if `no-misused-promises` is turned on (a stricter rule, not a loosened one) | E2E: `web/e2e/specs/errorHandling.spec.ts` (`web e2e (mock)`): a role-name-conflict case and a viewer Start, each asserting a toast appears; a unit test asserting Settings' Reload rejection is handled and shown | no | no — reuses the toasts the neighbouring flows already show | Group 4 also edits `Servers.tsx`; land after it |
| 23 | `fix/018-gp-module-validator-schema` | `type: fix`, `area: modules` | F-159 CRD-correct enum fields fail validation while `boolean`/no-options pass; F-160 `type: boolean` passes `validate` but the CRD only accepts `bool`; F-161 CRD-required fields (`ports[].name`, `displayName`, `game`, `image`, `version`) can be missing and still validate clean; F-162 `preview` with no version choice skips the version env layer the operator always applies | `gp-module/internal/validator/*.go` (enum/options/type-set checks, required-field checks), `gp-module/internal/preview/*.go` (default-version resolution) | Unit: `gp-module` validator test fixtures for each of the four gaps (an enum field matching the CRD shape, a `type: boolean` module, a module missing each required field, a preview with no `--version-id`) | yes — `gp-module/specs.md` validator coverage claims | no | none — but this fix should land before any new module authoring guidance ships, since it's the tool that gates module correctness |
| 24 | `fix/018-sentinel-core-reliability` | `type: fix`, `area: optional-components`, `area: operator` | F-179 (S3) a session the sentinel handed through is dropped the moment the game pod goes Ready, instead of surviving the flip-back as documented; F-180 (S3) a listener bind failure on one port is held silently while other listeners stay healthy, so the dead port is never logged | `operator/internal/controller/gameserver_sentinel.go` (`planSentinel`/`reconcileSentinel` delete the waker as soon as the game is Ready), `sentinel/main.go` (`run`, `serveTCP`, `serveUDP`, `proxyBidirectional`) | E2E: `bot-fast` bucket — extend `TestGameServer_WakeOnConnect_LoginWakes` so the client that woke the server stays connected past the game pod's Ready flip. Unit: a sentinel test asserting a fatal per-listener error is surfaced (returned or logged), not held until shutdown | yes — `sentinel/specs.md` invariant 3 (proxy waits for both directions) | no | none |
| 25 | `fix/018-capture-sidecar-retention` | `type: fix`, `area: optional-components` | F-187 (S3) retained (not-yet-expired) capture files can push the 1 GiB emptyDir over its limit, causing a kubelet eviction that restarts the game pod and loses every capture | `capture-sidecar/internal/httpserver/handlers.go` (`HandleStart` should account for existing retained bytes, not just the new capture's own `maxSizeBytes`), `operator/internal/controller/gameserver_controller.go` (emptyDir sizing) or `charts/gameplane/values.yaml` retention defaults | E2E: `operator` bucket capture case (next to `TestGameServer_NetworkCapture*`) asserting a second capture is refused/pre-empted rather than allowed to push total usage past the volume's `SizeLimit` | yes — `capture-sidecar/specs.md:24,241` | no | Live re-check needs `capture.enabled=true` for the round: OD-021 item 17 |
| 26 | `fix/018-mcp-server-log-truncation` | `type: fix`, `area: shared` | F-204 (S3) `get_pod_logs` truncates by keeping the oldest bytes of the requested tail instead of the newest, and doesn't say it truncated | `mcp-server/internal/kube/client.go` (`PodLogs`), `mcp-server/tools.go` (`getPodLogsHandler`) | Unit: `mcp-server/internal/kube/client_test.go` (`TestPodLogs`) with a log-heavy fixture asserting the returned text ends at the true tail and carries a truncation notice when the byte cap applies | yes — `mcp-server/specs.md` | no | none |
| 27 | `fix/018-charts-observability-scrape` | `type: fix`, `area: chart` | F-216 (S3) PodMonitor has no TLS config while the agent only serves `/metrics` over mTLS, so every agent scrape target is `down`; F-217 (S3) telemetry-receiver's `/metrics` shares the ingress-restricted port with `/ingest`, so no in-cluster Prometheus can reach it | `charts/gameplane/templates/servicemonitors.yaml` (the PodMonitor is rendered here), `charts/gameplane/templates/telemetry-receiver.yaml` (NetworkPolicy exception or a separate metrics listener) | Render checks in the `chart render` CI job: the rendered PodMonitor carries `scheme: https` + client cert secretRef; a Prometheus-namespace ingress rule exists for the receiver's metrics path | yes — `docs/install.md:239-241` | no | none |
| 28 | `fix/018-module-source-ref` | `type: fix`, `area: chart` | F-028 (S3) default git module source still pinned to `ref: v0.2.0-beta.6`; F-219 (S3) default catalog lists 16 modules, 14 spec-015 modules missing; F-222 (S4) `docs/install.md:176` documents that same `ref` default as `main` | `charts/gameplane/values.yaml:473,480-496`, `docs/install.md:176` | E2E: `operator` bucket `TestModuleSourceAndModule`-style assertion that the default catalog lists all 30 shipped modules once repointed | no (values plus one docs line; the README/docs and website counts are groups 50 and 51) | no | Branch name is the one T054 sets. **Blocked on the `gameplane-module` `v0.3.0` tag**: T054 (resolved 2026-09-24) cuts it only after the live `INV-MOD-*` rows pass, and confirms it with the maintainer before it is pushed. Do not open this PR until that tag exists |
| 29 | `fix/018-deploy-dev-tooling-reliability` | `type: fix`, `area: shared` | F-232 (S3) `dev-up` re-run doesn't pin `--context`, so it can touch whatever cluster is currently selected; F-234 (S3) a stopped-but-not-removed `kind-registry` container aborts bootstrap instead of being restarted; F-255 (S3) `dev-load` only loads 4 of the 12 built images, so opt-in components silently fail to pull on Kind; F-237 (S4) `CLAUDE.md` says `dev-load` rebuilds when it only loads (the other half of the same `dev-load` defect) | `deploy/kind/up.sh`, `Makefile` (`dev-load`, `dev-up`), `CLAUDE.md:104` | No CI job runs `make dev-up` or `dev-load`, so there is no job to extend. Either a new `hack/` check asserting `dev-load` loads every image `make images` builds, or a manual run recorded in evidence | no | no | `CLAUDE.md` is also edited by in-flight PR #421 (F-030); rebase after it merges |
| 30 | `fix/018-workflows-ci-reporting-triggers` | `type: ci`, `area: shared` | F-238 (S3) `capture-sidecar-setcap-proof` missing from `NEEDS_ORDER`/`JOB_MATCHERS`, so its failure is invisible in the PR summary; F-239 (S3) `publish-edge.yaml` path filters miss several images' real inputs (`sentinel/**`, `capture-sidecar/**`, `tunnel/**`, `gameaction/**`, `gameproto/**`, `svcutil/**`, `gp-module/**`), so `:edge` silently goes stale; F-240 (S3) the doc-gate filters skip `Chart.yaml` appVersion bumps and docs' link targets, so drift merges green; F-241 (S3) editing `.golangci.yml` doesn't trigger any lint job; F-244 (S3) `gp-module` missing from Dependabot's `gomod` directories; F-242 (S4) `ratelimit` bucket missing from the report's `bucketSet`; F-243 (S4) the `coverage/web` status step posts a hard-coded `success` (F-242 and F-243 edit the same report job as F-238) | `.github/workflows/ci.yaml` (`NEEDS_ORDER`, `JOB_MATCHERS`, `bucketSet`, the `coverage/web` status step, path filters), `.github/workflows/publish-edge.yaml` (paths list), `.github/dependabot.yml`; F-239's `main`→`master` wording at `docs/install.md:29` and `charts/gameplane/values.yaml:19` | No workflow self-test exists yet (spec 008's `contracts/permissions-matrix.md:58-60` is a manual checklist). Either a new `hack/` check (every `needs:` job appears in `NEEDS_ORDER`/`JOB_MATCHERS`; every image's `COPY` inputs appear in `publish-edge.yaml`'s paths) or a live CI run is the regression; F-243's posted state must follow the vitest coverage exit code | yes — spec 008's `data-model.md` invariants this violates | no | In-flight PR #420 edits the same `docs` filter (F-240 step 4); rebase after it merges |
| 31 | `fix/018-charts-docs-drift` | `type: docs`, `area: chart` | F-252 (S3) OIDC doc examples give `clientSecretRef` as a bare string (real schema is an object) and omit `enabled: true`, so a copied example fails `helm template`; F-220, F-221, F-223, F-224, F-225, F-226 (S4) CRD-doc/upgrade contradiction, capture buffer-vs-emptyDir sizing note, OIDC `displayName` key missing from chart, S3 region doc mismatch, `podSecurity.enforceRestricted` doc promises a nonexistent per-pod opt-in, incomplete image list | `docs/oidc.md`, `docs/install.md`, `docs/security.md`, `charts/gameplane/values.yaml` (comments); `charts/gameplane/templates/api.yaml` if F-223 is fixed by wiring `api.oidc.displayName` to `--oidc-display-name` (then label `type: fix`) | Render check in the `chart render` CI job with the corrected OIDC example values and `api.oidc.enabled: true` (fails today, per F-252); the rest is docs only | yes — the doc pages themselves are the fix | no | none |
| 32 | `docs/018-install-rollback` | `type: docs`, `area: chart` | F-027 (S3) `docs/install.md` has no rollback procedure | `docs/install.md` | None beyond the doc addition itself; T063 already owns this work | n/a (the finding *is* a specs/docs gap) | no | **Already scheduled as T063** (branch and labels are T063's). T063 needs T062's live rollback steps, T062 follows T061, and T061 is blocked on OD-023. This row exists so F-027 isn't dropped from the plan, not to duplicate T063 |

### Severity S4 — cosmetic, documentation, or wording

Grouped per component; code-behaviour S4 items are listed with their component's doc drift since they share files and reviewers. All of these come after every S1–S3 group above. Groups that change code come first (auth, lifecycle and backup paths leading), and docs-only groups come last.

| # | Branch | Labels | Findings | Files likely touched | Regression test | specs.md? | design.pen? | Dependencies |
|---|--------|--------|----------|----------------------|------------------|-----------|-------------|--------------|
| 33 | `fix/018-api-error-handling-and-docs` | `type: fix`, `area: api` | F-076 missing-config-row returns 500 instead of 200 (string-compares `sql.ErrNoRows` wrong); F-077 hand-written errors mapped to 500 instead of their 400/409; F-082 `RevokeShareLink` 500s on unknown id instead of 404; F-083 201 responses use `text/plain`; F-085 backfilled timestamps aren't RFC 3339; F-086–089 `api/specs.md` endpoint count, capture/audit/RBAC statements, session/limits/roles statements, and dependency/flag lists all stale | `api/internal/handlers/config.go`, `destinations.go`, `shares.go`, `modules.go`, `roles.go` and the other 201 routes F-083 lists, `api/internal/httperr/httperr.go`, `api/internal/db/shares.go`, `api/internal/db/preferences.go`, `api/specs.md`. F-085: migrations are append-only (CLAUDE.md "Add a Database Migration"), so the fix is a new migration or a read-side normalisation, never an edit to `011_user_theme_preferences.sql` | Unit: `httperr` classification test covering `sql.ErrNoRows` and hand-written errors; a `RevokeShareLink` unknown-id case asserting 404; a `GetPreferences` case asserting RFC 3339 for a backfilled row | yes | no | Lands after group 19, which edits the same `shares.go` files |
| 34 | `fix/018-operator-code-cleanup-and-docs` | `type: fix`, `area: operator` | F-051 capture expiration blocks CR delete on unreachable sidecar; F-055 `TunnelHostnameIgnored` condition never cleared; F-056 address not validated before sending to MetalLB/Cilium; F-057 idle-window parse error surfaces only in status, no condition; F-058 four unused capture config fields with misleading comments; F-059, F-062 `operator/specs.md` contradicts code in several places; F-060 seven CLI flags missing from the specs.md table; F-061 specs.md dependency and Go versions stale vs `go.mod`; F-063 documented dev config-path samples fail | `operator/internal/controller/*.go`, `operator/specs.md`, `operator/config/` samples | Unit tests for F-051/055/056/057 behaviour changes; the F-058–063 items are pure doc/dead-code cleanup, no new test | yes | no | Live re-check of F-051 needs `capture.enabled=true` for the round (OD-021 item 17); OD-021 item 22 (the `Resuming` and `Stopped` phases are never set) bears on how F-062's fix describes phases |
| 35 | `fix/018-agent-quiesce-and-docs` | `type: fix`, `area: agent` | F-107 quiesce rollback skipped when the *first* command fails (doc promises rollback on any failure) — **note: `TestDeclaredQuiescer_FirstCommandErrorSkipsRollback` pins the current behaviour on purpose, so this needs explicit sign-off on which side is wrong before any test changes** (CLAUDE.md override 1); F-109 nine spec/code contract differences; F-110 stale dependency versions, false "intentional pin" note; F-111 doc comments contradict current code | `agent/internal/quiesce/quiesce.go`, `docs/module-authoring.md:1109-1111` (the rollback promise F-107 cites), `agent/specs.md`, `agent/openapi.yaml` (F-109), and the stale comments F-111 names (`players.go`, `rcon.go`, `heartbeat.go`, `satisfactory.go`) | If F-107's resolution changes behaviour: update (never delete) `TestDeclaredQuiescer_FirstCommandErrorSkipsRollback` per the maintainer's ruling, plus a new case for the chosen behaviour; F-109–111 are docs-only | yes | no | **F-107 is a ⛔ approval item**: log it in `OPEN-DECISIONS.md` and wait for the maintainer's ruling (doc-is-wrong vs. code-is-wrong) before touching the pinned test. Its live check reaches quiesce only through a Backup (OD-021 item 2) |
| 36 | `fix/018-web-ui-polish` | `type: fix`, `area: web` | F-128 Modules hash-nav lands on the wrong section; F-129 "Add cluster" links to an unusable page; F-133 invite dialog retains the previous entry; F-134 Identity-providers tab copy contradicts the shipped Authentication feature | `web/src/routes/AdminSettings.tsx` (`initialSection` reads only `?section=`), `web/src/routes/Modules.tsx`, `web/src/components/ClusterSelector.tsx`, `web/src/routes/Users.tsx` (invite dialog, Identity providers tab) | E2E: one case per finding in `web/e2e/specs/modules.spec.ts`, `cluster.spec.ts` and `users.spec.ts` (`web e2e (mock)`) | no | **yes** for F-129 (removing or relabelling the menu item, or a new registration flow) and F-134 (tab copy on a designed screen, which the `design vs browser visual diff` job compares); no for F-128 and F-133 | none |
| 37 | `fix/018-web-code-quality-and-docs` | `type: fix`, `area: web` | F-135 API timestamps keyed wrong; F-136 typo "Baning"; F-138 dead code in exports/branches; F-139, F-140 `web/specs.md` contradicts/over-claims implemented UI; F-141 component version claims drift | `web/src/routes/AdminLogs.tsx` (log parser reads only `ts`), `web/src/routes/tabs/Players.tsx` ("Baning…"), dead code per F-138's grep hits, `web/specs.md`, `CLAUDE.md:78,262` ("React 18", F-141) | Unit: `web/src/routes/AdminLogs.test.tsx` fed an slog line with `time`, asserting the timestamp renders (F-135); the rest is dead-code and docs cleanup | yes | no | `CLAUDE.md` is also edited by in-flight PR #421 and group 29; rebase after them |
| 38 | `fix/018-gameaction-validation-and-docs` | `type: fix`, `area: shared` | F-149 parameter length cap counts bytes not characters; F-150 required parameter with a default accepts an empty value; F-151 template spec claims a non-deterministic function that doesn't exist | `gameaction/action.go`, `gameaction/specs.md` | Unit: a multi-byte-character param-length case; a required-param-with-default-empty-value case | yes | no | none |
| 39 | `fix/018-gameproto-validation-and-docs` | `type: fix`, `area: shared` | F-153 `Classify` error returns differ from the spec's promise; F-154 Terraria version string can exceed the documented 32 KB cap; F-155 `BuildStatusResponse` skips its documented JSON-validation contract; F-156 package-doc example code doesn't compile; F-157 `specs.md` layout/tests/deps/versions stale; F-158 adding `WakeProtocol` needs an undocumented CRD enum change | `gameproto/*.go`, `gameproto/specs.md` | Unit: a Terraria version string at/over 32 KB asserting the documented cap holds; a `Classify` error-shape test against the spec | yes | no | none |
| 40 | `fix/018-gp-module-docs-and-tooling` | `type: fix`, `area: modules` | F-163 archetype docs contradict a validation requirement; F-164 documented CLI/`make` commands fail; F-165 the documented image-pin command doesn't exist; F-166 `preview` flag/output format don't match the contract; F-167 specs file lists nonexistent files/dependencies; F-168 version-mismatch warning not implemented | `gp-module/**`, `gp-module/specs.md`, `docs/module-authoring.md`, `Makefile` (`module-validate`, `module-preview`, `module-package`, `module-pin`), `specs/010-easy-module-building/contracts/cli-contract.md` if the contract rather than the CLI changes | F-168 (missing warning) gets a unit test; the rest are docs corrections | yes | no | depends on group 23 landing first (same validator/preview code, avoid merge conflicts) |
| 41 | `fix/018-sentinel-code-and-docs` | `type: fix`, `area: optional-components` | F-181 startup error exits 0 instead of non-zero; F-182 close errors logged on healthy proxied connections; F-183 hostport hold-window asymmetry undocumented; F-184 UDP source-keying/cooldown counting misdocumented; F-185 four `specs.md` statements contradict code; F-186 dependency/test/doc references don't resolve; F-254 untracked `sentinel/sentinel` binary with no `.gitignore` entry | `sentinel/main.go`, `sentinel/specs.md`, a new `sentinel/.gitignore` (`/sentinel`, like `mcp-server/.gitignore`) | Unit: F-181 (exit code) gets a test; F-182 is a log-noise fix, easy to assert; F-254 is a one-line `.gitignore` add, no test needed | yes | no | should land after group 24. The `sentinel/sentinel` binary is untracked, so no commit removes it: the `.gitignore` entry is the fix, and deleting the local file is a local cleanup outside the PR |
| 42 | `fix/018-capture-sidecar-code-and-docs` | `type: fix`, `area: optional-components` | F-188 environment variables ignored, works only by flag-default coincidence; F-189 specs.md lists 4 endpoints, code has 6 (delete endpoint undocumented); F-190 four spec contradictions; F-191 gopacket dependency drift (fork vs upstream, version mismatch); F-192 409 error message names the wrong capture on status-poll failure; F-193 `/healthz` requires mTLS despite comments calling it unauthenticated | `capture-sidecar/cmd`, `capture-sidecar/internal/**`, `capture-sidecar/specs.md`; the stale comment at `api/internal/handlers/capture.go:840-848` (F-189); `operator/internal/controller/gameserver_controller.go:1628-1630` if the operator passes flags instead of env (F-188) | Unit: F-188 (env vars ignored) and F-192 (wrong capture named in error) each get a small regression test | yes | no | should land after group 25; live re-check needs `capture.enabled=true` (OD-021 item 17) |
| 43 | `fix/018-audit-syslog-bridge-code-and-docs` | `type: fix`, `area: shared` | F-195 specs.md references wrong files for the webhook sender; F-196 untested reconnect/write-deadline claims in specs.md; F-198 RFC 5424 APP-NAME validation missing | `audit-syslog-bridge/*.go` (reject or sanitise an invalid `APP_NAME`/`SYSLOG_HOSTNAME` at startup), `audit-syslog-bridge/specs.md` | F-198 (missing validation) gets a unit test; the rest are docs | yes | no | none |
| 44 | `fix/018-mcp-server-code-and-docs` | `type: fix`, `area: shared`, `area: chart` | F-205 examples use nonexistent label keys; F-206 malformed `labelSelector` silently returns the full list; F-207 docs claim 7 CRDs, 9 exist; F-208 specs.md dependency versions stale; F-209 chart comment references a nonexistent file path; F-210 README example fails on a typical Linux host | `mcp-server/tools.go`, `mcp-server/fixadvice.go`, `mcp-server/main.go`, `mcp-server/specs.md`, `mcp-server/README.md`, `charts/gameplane/templates/mcp-server.yaml`, `charts/gameplane/values.yaml:395` | F-206 gets a unit test asserting a malformed selector returns a tool error, as the sibling tools do; the rest are docs | yes | no | should land after group 26 |
| 45 | `fix/018-deploy-hack-scripts` | `type: fix`, `area: shared` | F-233 `e2e.sh` message/header cites the wrong image prefix and an incomplete image list; F-236 Unicode punctuation stripped from anchor slugs | `deploy/kind/e2e.sh:11,165`, `hack/check-links.sh:166` | F-236 is a logic bug in the link checker: a fixture case for `github_slug` with `→`/`—` headings. `hack/` has no link-checker harness yet (compare OD-012 for `check-doc-versions.sh`), so it's a new test. F-233 is message text only | no (script comments, not a `specs.md`) | no | none |
| 46 | `fix/018-svcutil-docs` | `type: docs`, `area: shared` | F-170 docs claim adoption that didn't happen; F-171 specs outdated (missing sections, wrong test count) | `svcutil/specs.md`, and the other adoption claims F-170 counts (`capture-sidecar/specs.md:58,88`, the orphan `replace` in `capture-sidecar/go.mod:11-13`, `capture-sidecar/Dockerfile:4-7`) | none — docs only | yes | no | Touches `capture-sidecar/specs.md` like group 42; land after it |
| 47 | `fix/018-tunnel-docs` | `type: fix`, `area: optional-components` | F-031 `docs/tunnels.md` examples use `spec.template` instead of `templateRef.name` (fail `kubectl apply` as written); F-175 troubleshooting docs use the wrong label selector; F-176 specs file misses sections, lists a stale dependency | `docs/tunnels.md` (examples `:118-213`, troubleshooting `:304`, `:325-328`), `tunnel/specs.md`, `tunnel/Dockerfile.{frp,tailscale,playit}` (stale `gameaction/` copy and comment, F-176) | none beyond F-031's examples, which are worth a `kubectl apply --dry-run=server` check against the CRDs if the repo has a doc-example linter | yes | no | should land after group 8 so the docs describe the fixed behaviour, not the buggy one |
| 48 | `fix/018-telemetry-receiver-docs` | `type: docs`, `area: shared` | F-201 specs.md claims untested HTTP-method guards | `telemetry-receiver/specs.md` (or `telemetry-receiver/main_test.go` if the guard tests the doc claims are added) | none — docs only (or add the HTTP-method-guard test the doc already claims exists) | yes | no | none |
| 49 | `fix/018-test-e2e-docs-drift` | `type: docs`, `area: e2e` | F-040 fast-game-set definitions conflict across `fastGameSet`/`buckets.sh`/specs.md; F-041 `specs.md` depth table has 16 rows, repo has 29 probe packages; F-042 `api-roles` bucket's documented admin-login ceiling (≤~5) undercounts the real ~7 | `test/e2e/internal/specs.md`, `test/e2e/buckets.sh` comments, `.claude/skills/e2e-test-authoring/SKILL.md:17,27`, the `api_auth_e2e_test.go:967` comment; `test/e2e/gamebot_helpers_e2e_test.go:24-32` (`fastGameSet`) if F-040's canonical list changes code | none — docs only, except that changing `fastGameSet` is a test-file change; F-040's conflict should resolve to one canonical list, which is itself the fix | yes — `test/e2e/internal/specs.md` | no | none |
| 50 | `fix/018-docs-module-and-examples-drift` | `type: docs`, `area: shared` | F-032 README/roadmap/comparison cite 16 modules, repo has 30; F-033 `security.md` misstates the capture default as `true`; F-034 `comparison-sources.md` citations no longer match CLAUDE.md line numbers; F-035 `dependencies.md` missing 6 Go modules; F-036 `contributing.md` per-component test list missing the same 6; F-245 `contributing.md`'s actionlint command uses the wrong extension glob | `README.md`, `docs/roadmap.md`, `docs/security.md`, `docs/comparison-sources.md`, `docs/dependencies.md`, `docs/contributing.md` (`:49-50` and `:74-83`) | none — docs only | no | no | module-count items (F-032) should land together with group 28's catalog expansion so the count is corrected once, not twice |
| 51 | `fix/018-website-catalog-version` | `type: docs`, `area: shared` | F-037 website games page lists 16, 30 shipped; F-038 `comparison.mdx` says "16 official modules"; F-039 website `VERSION` constant still `beta.7` | `website/` (submodule — commit there first, then bump the submodule pointer per CLAUDE.md's "Update Public Website" workflow) | none — docs/content only | no | no | **depends on group 28** (module source ref / new tag) for the count, and on the status-wording work (T071) for the version constant — do not fix the version half until the v0.3.0 status-wording commit is ready, to avoid a double edit |
| 52 | `fix/018-go-version-docs` | `type: docs`, `area: shared`, `area: specs` | F-253 README.md / plan.md say "Go 1.25" vs go.mod's 1.26 requirement; F-152 `gameaction/specs.md` says Go 1.25; F-197 `audit-syslog-bridge/specs.md` says Go 1.25; F-200 `telemetry-receiver/specs.md` says Go 1.25 (F-200 and F-253 name these as one pattern to fix together) | `README.md:230`, `specs/018-v0-3-release-readiness/plan.md:20`, `gameaction/specs.md:5`, `audit-syslog-bridge/specs.md:55`, `telemetry-receiver/specs.md:5`, and F-152's related sites `test/e2e/internal/specs.md:5,262`. The Go lines inside F-061 (`operator/specs.md`) and F-157 (`gameproto/specs.md:5`) stay with groups 34 and 39 | none — docs only | yes — the `specs.md` lines are the fix | no | Groups 38, 43, 48 and 49 edit other lines of the same files; whichever lands second rebases |

---

## Proposed closures

None. Every open finding read for this plan describes a real, reproducible defect (a behaviour that contradicts its own spec, its own doc, or its own design intent) rather than a documented, intentional trade-off placed after v0.3 in `docs/roadmap.md`. No `not-a-defect` or `out-of-scope` justification met the bar on the evidence available, so none is proposed here.

One related note for T049, not a closure proposal here: F-020 (currently `imported`, not `open`) already carries a "believed not-a-defect" note in `findings.md` tying it to FR-004's documented contingency. It stays entirely in T049's re-check lane above; if T049 confirms it, the *next* triage round is where a `not-a-defect` closure would actually get proposed and signed off — not this one, since F-020 isn't `open` yet.
