# Go
/bin/
/dist/
*.test
*.out
*.prof
coverage.*
coverage/
vendor/
operator/bin/
test/e2e/.kube/

# Node / web
/node_modules/
web/node_modules/
web/dist/
web/.vite/
web/.turbo/
web/coverage/
web/playwright-report/
web/test-results/
web/e2e/.portforward.pid
*.log
.npmrc.local

# Editor / OS
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
.DS_Store
Thumbs.db
*.swp
*~

# Env / secrets
.env
.env.*
!.env.example
# web/.env.mock is e2e-only (opts the SPA into MSW) and contains no
# secrets — it must be tracked so the CI mock-mode job can load it.
!.env.mock
*.pem
*.key
kubeconfig
kubeconfig-*

# Build / dev
/tmp/
/.cache/
/.local/

# Kind / k3d artifacts
/.kube/

# Claude Code local state
.claude/settings.local.json
.claude/scheduled_tasks.lock
cosign.key
.claude
.codex
.agents
